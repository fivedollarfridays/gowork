---
id: T12.14
title: Worker Voice + Templates
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P1
complexity: 20
status: pending
sprint: 12b
tags:
  - backend
  - documents
depends_on:
  - T12.4
---

# Objective

Create `backend/app/modules/documents/voice.py`. Port voice helpers from ops — BOTH `cover_letter_generator.py` (`apply_voice_rules`, `_strip_dashes`, `_strip_hedges`, `_strip_quotes`) AND `resume_generator.py` (`apply_summary_voice_rules`, `_strip_hyphens`). Reading-level target <9.0 Flesch-Kincaid on a fixed 5-sample corpus, asserted in tests.

# Files to Update

| File | Change |
|------|--------|
| backend/app/modules/documents/voice.py | create — combined voice rule helpers |
| backend/app/modules/documents/templates/resume_base.md.j2 | create |
| backend/app/modules/documents/templates/cover_letter_base.md.j2 | create |
| backend/tests/test_document_voice.py | create (includes F-K corpus assertion) |

# Implementation Plan

1. Port dash/hedge/quote/hyphen strippers + dignified-substitution rules.
2. Implement `apply_worker_voice(text)` as the single entry point combining all rules.
3. Author the resume + cover-letter base templates as Jinja2 with `autoescape=True`.
4. Fixed 5-sample corpus in tests; assert F-K grade <9.0 on each after `apply_worker_voice`.
5. Tests cover template rendering and voice rule application.

# Acceptance Criteria

- [ ] `apply_worker_voice(text)` post-processor (dash/hedge/quote/hyphen stripping + dignified substitution)
- [ ] Both resume and cover-letter voice helpers ported
- [ ] Template files exist and render with Jinja2 (autoescape on)
- [ ] Flesch-Kincaid grade on fixed corpus of 5 sample outputs <9.0 — asserted in test
- [ ] Tests cover voice rule application + template rendering
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_document_voice.py -v
bpsai-pair arch check backend/app/modules/documents/voice.py
```
