---
id: T12.9
title: Pathway → Appointment Auto-Linker + Reconciliation
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 25
status: pending
sprint: 12a
tags:
  - backend
  - appointments
  - pathway
depends_on:
  - T12.7
---

# Objective

Create `backend/app/modules/appointments/barrier_linker.py`. Hook called from the two route callers (`routes/pathway.py:67`, `routes/plan_intelligence.py:80`) — NOT inside `generate_pathways()` (engine stays pure). Creates appointment placeholders for stages that require scheduled events. Ports `ops:lib/appointment_reconciliation.py` (`find_placeholder_matches`, `validate_patch`).

# Files to Update

| File | Change |
|------|--------|
| backend/app/modules/appointments/barrier_linker.py | create — placeholder generator + reconciliation |
| backend/app/routes/pathway.py | hook linker at line 67 area |
| backend/app/routes/plan_intelligence.py | hook linker at line 80 area |
| backend/tests/test_barrier_linker.py | create |

# Implementation Plan

1. Implement placeholder generation for: expunction, benefits recert, DMV, childcare intake, court dates.
2. Make city-aware: HHSC (Fort Worth) vs DHR (Montgomery) agency names.
3. Implement idempotency — reconcile against existing appointments.
4. Port `find_placeholder_matches(new_appointment)` returning candidate merge targets.
5. Port `validate_patch` to validate patch shape.
6. Insert hook into BOTH `routes/pathway.py` AND `routes/plan_intelligence.py` after plan generation.
7. Keep `generate_pathways()` signature unchanged — engine remains pure.
8. Write tests: AL + TX placeholder content, idempotency, manual-create-matches-placeholder reconciliation.

# Acceptance Criteria

- [ ] Placeholder generation covers expunction, benefits recert, DMV, childcare intake, court dates
- [ ] City-aware: correct state agency names (HHSC vs DHR)
- [ ] Idempotent — reconciles against existing
- [ ] Hook inserted at BOTH `routes/pathway.py` AND `routes/plan_intelligence.py` — test exercises both callers
- [ ] `generate_pathways()` signature unchanged — remains session-agnostic and pure
- [ ] `find_placeholder_matches(new_appointment)` returns candidate merge targets; `validate_patch` validates patch shape
- [ ] Tests verify AL + TX placeholder content, idempotency, reconciliation on manual-create-matches-placeholder
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_barrier_linker.py -v
bpsai-pair arch check backend/app/modules/appointments/barrier_linker.py
```
