---
id: T23.10
title: Sprint 23 integration gate
plan: plan-2026-05-s23-assessment-authoring
type: chore
priority: P0
complexity: 10
status: done
sprint: '23'
tags:
- assessments
- sprint-23
- gate
- integration
depends_on:
- T23.4
- T23.6
- T23.8
- T23.9
completed_at: '2026-05-07T18:00:58.797513'
---

# Objective

Final gate. Runs the full backend test matrix on sqlite + postgres (with T23.9's transaction-per-test isolation). Frontend vitest. Lint + arch + tsc + build. Verifies `auth.py` line count is unchanged from sprint start (the cross-task constraint). End-to-end smoke: drives a draft → review → approve → publish → public-fetch path through the actual route handlers in a single integration test.

# Files

- `backend/tests/test_assessments_e2e.py` (new — end-to-end smoke)
- `.paircoder/context/state.md` (reconcile)

# Acceptance Criteria

- [x] Full backend test suite green on sqlite locally + in CI
- [x] Full backend test suite green on postgres in CI (with T23.9 — identity-layer tests included)
- [x] Frontend vitest green
- [x] `ruff check .` clean on touched files
- [x] `bpsai-pair arch check .` no new violations
- [x] `auth.py` line count unchanged from S23 entry (assert via line-count diff in PR description)
- [x] `tsc --noEmit` 0 errors; `next build` green
- [x] `backend/tests/test_assessments_e2e.py` drives:
  1. POST /api/admin/assessments/draft (admin role)
  2. GET /api/admin/assessments/pending (sme_reviewer role)
  3. POST /api/admin/assessments/{version_id}/review action=approve
  4. POST /api/admin/assessments/{version_id}/publish (admin role)
  5. GET /api/assessments/{slug} (anonymous + claimed)
  6. Asserts provenance: drafted_by + reviewed_by + approved_by + published_at all populated on the published row
- [x] `.paircoder/context/state.md`: Sprint 23 complete entry with full task list; closed-follow-up note removed for postgres-test-isolation
- [x] PR pushed; CI green on both engine matrix axes

# Verification

- `cd backend && .venv/bin/python -m pytest tests --tb=short -q` (sqlite)
- `cd backend && .venv/bin/python -m pytest tests/test_assessments_e2e.py -v` (smoke focus)
- `cd frontend && npx vitest run && npx tsc --noEmit && npx next build`
- `ruff check .` and `bpsai-pair arch check .`
- `gh pr view <PR#> --json statusCheckRollup`
- `bpsai-pair task update T23.10 --status done`

# Dependencies

- T23.4 — reviewer queue active
- T23.6 — public fetch active (closes the read side of the chain)
- T23.8 — frontend RoleGate + nav active
- T23.9 — postgres CI parity for the identity layer + assessment chain