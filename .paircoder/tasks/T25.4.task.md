---
id: T25.4
title: Dallas barrier graph + training programs + childcare providers seed
plan: plan-2026-05-s25-dallas-expansion
type: feature
priority: P0
complexity: 18
status: done
sprint: '25'
tags:
- seed-data
- sprint-25
- barrier-graph
- training
- childcare
depends_on:
- T25.2
completed_at: '2026-05-08T19:04:06.561285'
---

# Objective

Author `data/cities/dallas/barrier_graph_seed.json`, `data/cities/dallas/training_programs.json`, and `data/cities/dallas/childcare_providers.json`. The barrier graph is structurally identical to FW (same nodes/edges, same relationship types) — only resource references swap to Dallas equivalents from T25.2's seed. Training programs and childcare providers are Dallas-specific from open sources.

The barrier-graph structural identity is load-bearing: it lets the matcher + barrier-intel modules treat Dallas + FW symmetrically. Drift surfaces in T25.6 validation tests.

# Files

- `data/cities/dallas/barrier_graph_seed.json` (new)
- `data/cities/dallas/training_programs.json` (new)
- `data/cities/dallas/childcare_providers.json` (new)
- `backend/tests/test_dallas_barrier_graph_parity.py` (new — structural diff with FW)

# Acceptance Criteria

- [x] `data/cities/dallas/barrier_graph_seed.json` is structurally identical to `data/cities/fort-worth/barrier_graph_seed.json` (same barrier IDs, same edges, same relationship types) with only resource references swapped to Dallas names from T25.2
- [x] Diff between FW + Dallas `barrier_graph_seed.json` (ignoring whitespace + resource_name fields) is empty for structural keys
- [x] `data/cities/dallas/training_programs.json` includes Dallas College (formerly DCCCD) campuses (≥3), Texans Can Academies (Dallas), Dallas Workforce Development apprenticeships
- [x] `data/cities/dallas/childcare_providers.json` includes ChildCareGroup centers, Dallas-area Texas Rising Star providers (≥10 entries)
- [x] All entries follow FW JSON shape (loaders are city-agnostic)
- [x] Resource references in `barrier_graph_seed.json` ALL appear in `community_resources.json` from T25.2 (verifiable via cross-file name-match in the parity test)
- [x] JSON parses clean, UTF-8 encoded
- [x] Structural parity test (`test_dallas_barrier_graph_parity.py`) asserts: same barrier IDs across FW + Dallas, same edge structure, all Dallas resource refs resolve in T25.2's community_resources

# Verification

- `cd backend && .venv/bin/python -c "import json; bg = json.load(open('../data/cities/dallas/barrier_graph_seed.json')); print(f'{len(bg)} barrier nodes')"`
- `cd backend && .venv/bin/pytest tests/test_dallas_barrier_graph_parity.py -v`
- `bpsai-pair task update T25.4 --status done`

# Dependencies

T25.2 — barrier graph references resource names from T25.2's `community_resources.json`; structural parity test cross-checks the resource references resolve.