---
id: T1.3
title: JobAdapter protocol and registry
plan: plan-sprint-1-engage
type: feature
priority: P0
complexity: 15
status: done
sprint: '1'
depends_on: []
completed_at: '2026-04-11T15:37:05.655454'
---

# JobAdapter protocol and registry

Create `backend/app/integrations/adapters/` package. Define a `JobAdapter` Protocol with `async def fetch_jobs(self, session, query: str, location: str) -> list[dict]`. Implement a `get_adapter(name: str) -> JobAdapter` registry with lazy imports so concrete adapter modules can register without circular dependencies. Raise `AdapterNotFoundError` for unknown adapter names.

# Acceptance Criteria

- [x] `JobAdapter` is a `typing.Protocol` with `async def fetch_jobs(self, session, query: str, location: str) -> list[dict]`
- [x] `get_adapter("brightdata")` returns the BrightData adapter (implemented in T1.4)
- [x] `get_adapter("unknown")` raises `AdapterNotFoundError`
- [x] No runtime imports of BrightData symbols in `base.py` — registry uses lazy imports so T1.4/T1.5 can register without circular deps