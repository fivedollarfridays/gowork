---
id: T24.5
title: Employer intake endpoint
plan: plan-2026-05-s24-listing-verification
type: feature
priority: P0
complexity: 20
status: done
sprint: '24'
tags:
- verification
- sprint-24
- intake
- employer
depends_on:
- T24.4
completed_at: '2026-05-08T09:22:44.307082'
---

# Objective

`POST /api/employers/{employer_account_id}/listings/{listing_id}/intake` accepts the structured intake answers (must-haves, day-1 tasks, comp band, fair-chance willingness). Gated on the `gw_employer_account` cookie matching the path's employer_account_id, OR admin role override. Pydantic-validated payload; comp_band_min ≤ comp_band_max sanity guard. Persists intake_json on listing_verifications + sets intake_completed_at.

# Files

- `backend/app/routes/employers.py` (extend)
- `backend/tests/test_employers_intake.py` (new)

# Acceptance Criteria

- [x] `POST /api/employers/{employer_account_id}/listings/{listing_id}/intake` route added
- [x] Body: `{must_haves, nice_to_haves, real_day1_tasks, comp_band_min, comp_band_max, fair_chance_willingness, additional_notes}`
- [x] Pydantic validation: comp_band_min ≤ comp_band_max, both positive; must_haves + real_day1_tasks non-empty
- [x] Gated: gw_employer_account cookie matches employer_account_id (cookie-verified) OR admin role
- [x] Persists intake_json + sets intake_completed_at via set_intake
- [x] Returns updated verification record (intake_json INCLUDED — caller is the employer)
- [x] Tests cover: happy path, comp_band sanity guard (max < min → 422), anonymous → 403, non-owner → 403, admin override succeeds
- [x] bpsai-pair arch check passes

# Verification

- `cd backend && .venv/bin/python -m pytest tests/test_employers_intake.py -v`
- `wc -l backend/app/routes/auth.py` — 314
- `bpsai-pair arch check backend/app/routes/employers.py`
- `bpsai-pair task update T24.5 --status done`

# Dependencies

- T24.4 — verification record must exist before intake can target it.