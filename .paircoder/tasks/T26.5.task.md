---
id: T26.5
title: Frontend admin_resources API client
plan: plan-2026-05-s26-admin-dashboard
type: feature
priority: P0
complexity: 12
status: pending
sprint: '26'
tags:
- frontend
- sprint-26
- api-client
depends_on:
- T26.2
---

# Objective

Typed API client for the resource CRUD endpoints. Mirrors the S25 `cities_admin.ts` shape post-/reviewing-and-fixing lift: imports `fetchWithCookie` + `throwOnApiError` from `_client.ts` (the shared helpers added in the post-PR-#126 simplify pass); defines a single typed `AdminResourcesApiError`; one function per endpoint. Each per-domain client subclass keeps only its typed error subclass.

# Files

- `frontend/src/lib/api/admin_resources.ts` (new)
- `frontend/src/lib/api/__tests__/admin_resources.test.ts` (new)

# Acceptance Criteria

- [ ] `frontend/src/lib/api/admin_resources.ts`:
  - `import { fetchWithCookie, throwOnApiError } from "./_client"`
  - Typed wire interfaces: `Resource`, `ResourceListResponse` (with pagination `{items, total, limit, offset}`), `CreateResourcePayload`, `UpdateResourcePatch`
  - `AdminResourcesApiError extends Error` with `status` + `detail`
  - Functions: `listResources(opts: {city, limit?, offset?, includeHidden?})`, `getResource(id)`, `createResource(payload)`, `updateResource(id, patch)`, `hideResource(id)` (DELETE), `restoreResource(id)` (POST /restore)
- [ ] All functions delegate to `fetchWithCookie` + `throwOnApiError(res, AdminResourcesApiError, "admin-resources")`
- [ ] `frontend/src/lib/api/__tests__/admin_resources.test.ts` — vitest stubs `fetch` and asserts URL + method + body for each function; verifies typed error throws on non-2xx (parsed `detail` string)
- [ ] `npx tsc --noEmit` clean (run from `frontend/`)
- [ ] vitest suite green (no regressions vs S25 baseline 3629 passed)
- [ ] No new files under `frontend/src/components/` (per task scope; UI components belong to T26.8)

# Verification

- `cd frontend && npx vitest run src/lib/api/__tests__/admin_resources.test.ts`
- `cd frontend && npx tsc --noEmit`
- `bpsai-pair task update T26.5 --status done`

# Dependencies

T26.2 — needs the backend route shape (paths + payloads + response shapes) to land first.
