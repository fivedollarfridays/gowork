---
id: T26.5
title: Frontend admin_resources API client
plan: plan-sprint-26-engage
type: feature
priority: P0
complexity: 12
status: done
sprint: '26'
depends_on:
- T26.2
completed_at: '2026-05-09T10:30:04.177951'
---

# Frontend admin_resources API client

Typed API client for the resource CRUD endpoints. Mirrors the S25 `cities_admin.ts` shape post-/reviewing-and-fixing lift: imports `fetchWithCookie` + `throwOnApiError` from `_client.ts` (the shared helpers added in the post-PR-#126 simplify pass); defines a single typed `AdminResourcesApiError`; one function per endpoint. Each per-domain client subclass keeps only its typed error subclass.

# Acceptance Criteria

- [x] `frontend/src/lib/api/admin_resources.ts` (new):
- [x] All functions delegate to `fetchWithCookie` + `throwOnApiError(res, AdminResourcesApiError, "admin-resources")`
- [x] `frontend/src/lib/api/__tests__/admin_resources.test.ts` (new) — vitest stubs `fetch` and asserts URL + method + body for each function; verifies typed error throws on non-2xx
- [x] `npx tsc --noEmit` clean
- [x] vitest suite green (no regressions)
- [x] No new files under `frontend/src/components/` (per task scope; UI components belong to T26.8)