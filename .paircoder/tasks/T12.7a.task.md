---
id: T12.7a
title: Appointment Enrichment + Stage Advance
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P1
complexity: 20
status: pending
sprint: 12b
tags:
  - backend
  - appointments
  - events
depends_on:
  - T12.7
---

# Objective

Port `ops:lib/appointment_merge.py` (`auto_advance_stage`, `merge_appointment`, `build_pipeline_summary`, `new_enrichment`, `enrichment_changed`) to `backend/app/modules/appointments/enrichment.py`. Subscribes to S12a's `appointment.attended` event (emitted by T12.7) — no direct import of T12.7. Port `ops:lib/conversion_triggers.py` event-hook pattern. When `auto_advance_stage` advances to a terminal stage, emit `barrier.cleared(session_id, barrier_id)` for intelligence-engine calibration.

# Files to Update

| File | Change |
|------|--------|
| backend/app/modules/appointments/enrichment.py | create — enrichment + stage advance logic |
| backend/app/modules/appointments/__init__.py | register subscriber to `appointment.attended` |
| backend/tests/test_appointment_enrichment.py | create |

# Implementation Plan

1. Port `auto_advance_stage(appointment)` — returns proposed stage change; does NOT mutate.
2. Port `merge_appointment(existing, new)` — detects field-level enrichment.
3. Port `build_pipeline_summary(session_id)` — aggregates per-session stage progress.
4. Wire event-bus subscriber to `appointment.attended` and `job_application.offer` (no direct import of emitters).
5. Emit `barrier.cleared(session_id, barrier_id)` when auto-advance reaches a terminal stage.
6. City-aware stage name resolution (AL expunction vs TX nondisclosure).
7. Write unit + integration tests covering all five shapes + barrier-cleared emission.

# Acceptance Criteria

- [ ] Subscribes to S12a event bus (`appointment.attended`, `job_application.offer`) — does NOT import emitters
- [ ] `auto_advance_stage(appointment)` returns proposed stage change (does NOT mutate — caller decides)
- [ ] `merge_appointment(existing, new)` detects field-level enrichment
- [ ] `build_pipeline_summary(session_id)` aggregates stage progress
- [ ] Emits `barrier.cleared(session_id, barrier_id)` when `auto_advance_stage` advances to terminal stage
- [ ] City-aware stage name resolution (AL expunction vs TX nondisclosure)
- [ ] Tests cover auto-advance per type, enrichment detection, summary, barrier-cleared emission
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_appointment_enrichment.py -v
bpsai-pair arch check backend/app/modules/appointments/enrichment.py
```
