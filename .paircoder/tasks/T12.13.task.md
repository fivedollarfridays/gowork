---
id: T12.13
title: Jobs API Routes
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 15
status: pending
sprint: 12a
tags:
  - backend
  - routes
  - jobs
depends_on:
  - T12.11
  - T12.12
---

# Objective

Create `backend/app/routes/jobs_applications.py`. Route prefix `/api/job-applications` — NOT `/api/jobs` (collision with existing `routes/jobs.py:13` which owns `/api/jobs/{job_id}`). Endpoints: `GET`, `POST`, `PATCH /{id}`, `GET /funnel?session_id=X`, `GET /community-funnel?segment_by=X`.

# Files to Update

| File | Change |
|------|--------|
| backend/app/routes/jobs_applications.py | create — 5 endpoints under /api/job-applications |
| backend/app/routes/__init__.py | register router |
| backend/tests/test_jobs_applications_routes.py | create |

# Implementation Plan

1. Implement five endpoints under prefix `/api/job-applications`.
2. Verify no collision with existing `/api/jobs/{job_id}` — add an integration test hitting both and asserting distinct handlers.
3. Community funnel respects city scoping AND T12.12 k-anonymity suppression.
4. Register router in `all_routers`.
5. Write tests covering auth, happy path, malformed input, route-resolution correctness.

# Acceptance Criteria

- [ ] All five endpoints under `/api/job-applications` prefix
- [ ] No collision with `/api/jobs/{job_id}` — integration test hits both and asserts distinct handlers (shape check)
- [ ] Community funnel respects city scoping AND T12.12 k-anonymity suppression
- [ ] Routes registered and tested
- [ ] Tests cover auth + happy path + malformed input + route-resolution correctness
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_jobs_applications_routes.py -v
bpsai-pair arch check backend/app/routes/jobs_applications.py
```
