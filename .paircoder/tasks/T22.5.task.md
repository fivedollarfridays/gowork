---
id: T22.5
title: accounts + account_sessions + account_credentials tables
plan: plan-2026-05-s22-identity-foundation
type: feature
priority: P0
complexity: 25
status: done
sprint: '22'
tags:
- identity
- sprint-22
- schema
- accounts
depends_on:
- T22.3
completed_at: '2026-05-06T17:14:34.190861'
---

# Objective

Introduce the identity layer. Create `accounts` (durable identity), `account_sessions` (link table from account to one or more `session_id`s), and `account_credentials` (magic-link tokens, future credential types). Add `backend/app/core/queries_accounts.py` with the CRUD surface. `account_id` is nullable on existing tables — anonymous-first invariant enforced.

# Files

- `backend/alembic/versions/0011_accounts.py` (new)
- `backend/app/core/queries_accounts.py` (new)
- `backend/tests/test_accounts.py` (new)

# Acceptance Criteria

- [x] `backend/alembic/versions/0011_accounts.py` creates the three tables
- [x] `accounts(id PK, email UNIQUE, created_at, last_active_at)`
- [x] `account_sessions(account_id FK, session_id, claimed_at, PRIMARY KEY(account_id, session_id))` with UNIQUE on `session_id` (one session belongs to at most one account)
- [x] `account_credentials(id PK, account_id FK, credential_type, credential_value_hash, expires_at, used_at)`
- [x] `backend/app/core/queries_accounts.py` with: `create_account`, `get_account_by_email`, `claim_session`, `list_sessions_for_account`, `get_account_for_session`
- [x] `backend/tests/test_accounts.py` covers create + claim + list + get_for_session paths
- [x] All queries work on both sqlite and postgres
- [x] `bpsai-pair arch check` passes on touched files

# Verification

- `cd backend && pytest tests/test_accounts.py` (sqlite + postgres)
- `bpsai-pair arch check backend/app/core/queries_accounts.py backend/tests/test_accounts.py`
- `bpsai-pair task update T22.5 --status done`

# Dependencies

- T22.3 — adds revision 0011 on top of the ported alembic chain.