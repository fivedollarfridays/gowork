---
id: T22.10
title: Frontend login surface (magic-link UX)
plan: plan-2026-05-s22-identity-foundation
type: feature
priority: P0
complexity: 20
status: pending
sprint: '22'
tags: [identity, sprint-22, frontend, auth]
depends_on: [T22.7, T22.8]
---

# Objective

Ship `/auth/login` and `/auth/claim` pages in the Next.js frontend. Login page accepts an email, POSTs to `/auth/magic-link`, and shows a "check your email" confirmation. Claim page consumes `?token=`, calls `/auth/claim`, and shows success or invalid-token state. vitest coverage for both.

# Files

- `frontend/src/app/auth/login/page.tsx` (new)
- `frontend/src/app/auth/claim/page.tsx` (new)
- `frontend/src/lib/api/auth.ts` (new)
- `frontend/src/__tests__/auth/login.test.tsx` (new)
- `frontend/src/__tests__/auth/claim.test.tsx` (new)

# Acceptance Criteria

- [ ] `/auth/login`: email form → POST → confirmation
- [ ] `/auth/claim`: reads `?token=`, calls API, renders success or invalid-token state
- [ ] `frontend/src/lib/api/auth.ts`: typed client functions `requestMagicLink(email)` and `claimMagicLink(token)`
- [ ] Both pages styled with shadcn/Tailwind matching existing GoWork palette (cyan/amber/rose)
- [ ] vitest coverage for both pages (rendering + form submission + error states) and the API client
- [ ] No build regressions: `npx next build` green; `npx tsc --noEmit` 0 errors

# Verification

- `cd frontend && npx vitest run src/__tests__/auth/`
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npx next build`
- Manual: visit `/auth/login`, submit email, check inbox, click link → claim succeeds
- `bpsai-pair task update T22.10 --status done`

# Dependencies

- T22.7 — login page POSTs to `/auth/magic-link`.
- T22.8 — claim page consumes `/auth/claim`.
