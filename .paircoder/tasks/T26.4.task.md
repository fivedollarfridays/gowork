---
id: T26.4
title: Migrate brightdata.py from require_admin_key to require_role(admin)
plan: plan-2026-05-s26-admin-dashboard
type: refactor
priority: P0
complexity: 10
status: pending
sprint: '26'
tags:
- refactor
- sprint-26
- auth
- brightdata
depends_on: []
---

# Objective

Refactor `backend/app/routes/brightdata.py` to drop the legacy `require_admin_key` (header-based) gate and use S22's `require_role("admin")` (cookie-based) instead. The endpoint surface (`POST /api/brightdata/crawl`, status endpoints, etc.) stays identical; only the auth mechanism changes. After T26.4, the new admin UI (T26.10) can call BrightData via the same cookie session as every other admin surface.

**Honest doc note (commit message):** this is a breaking change for any external script that called these endpoints with the legacy api-key. The endpoints are admin-only operator surface; external usage is unlikely but should be communicated. Out of scope: migrating the OTHER `require_admin_key` callers (`admin_flags.py`, `demo.py`) — different concerns, separate sprints.

# Files

- `backend/app/routes/brightdata.py` (modify — drop require_admin_key, add require_role)
- `backend/tests/test_brightdata.py` (extend or create)

# Acceptance Criteria

- [ ] All routes in `backend/app/routes/brightdata.py` gated by `require_role("admin")` (not `require_admin_key`)
- [ ] The `from app.core.auth import require_admin_key` import removed from this file
- [ ] All routes added to `_cross_session_fixtures.py:PUBLIC_ENDPOINTS` with rationale (per-route AC discipline)
- [ ] `backend/tests/test_brightdata.py` covers:
  - admin role with cookie session succeeds (mock the actual brightdata client)
  - non-admin role returns 403
  - anonymous returns 403 (matches S25 cities_admin contract — distinguished via `detail` string `"Authentication required"` vs `"Insufficient permissions"`)
- [ ] No regression: existing brightdata tests still pass (mock the actual brightdata client; only the gate behavior changes)
- [ ] `bpsai-pair arch check backend/app/routes/brightdata.py` passes (no new violations; line count likely shrinks slightly from removing the api-key dependency)
- [ ] `ruff check` clean on touched files
- [ ] Note: router registration in `routes/__init__.py` is OWNED BY T26.2 (the import path may be unchanged; T26.2 verifies)

# Verification

- `cd backend && .venv/bin/pytest tests/test_brightdata.py -v`
- `cd backend && .venv/bin/pytest tests/test_cross_session_isolation.py -v`  (verify brightdata routes are allowlisted)
- `grep -n "require_admin_key\|require_role" backend/app/routes/brightdata.py`  (confirm only require_role)
- `bpsai-pair arch check backend/app/routes/brightdata.py`
- `bpsai-pair task update T26.4 --status done`

# Dependencies

None — pure refactor of an existing route module. Cross-session allowlist registration self-contained in this task.
