---
id: T12.32b
title: S12b E2E Integration Tests
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P0
complexity: 25
status: pending
sprint: 12b
tags:
  - backend
  - testing
  - e2e
depends_on:
  - T12.17
  - T12.19
  - T12.24
  - T12.25a
  - T12.27
  - T12.28
  - T12.30
  - T12.31
  - T12.36
---

# Objective

Create `backend/tests/test_s12b_worker_companion_e2e.py`. Full-flow tests for S12b scope spanning: LLM-gating, fair-chance branching, SSRF-blocked PDF, transactional appointment emails, signed manage-link round-trip with replay + rotation, reminder multi-barrier dedup, plan refresher with progress carry-forward + history cap, past-appointment auto-advance + worker notice, compliance export + signed link, selective delete visibility, advisor inbox city-scope, demo exclusion.

# Files to Update

| File | Change |
|------|--------|
| backend/tests/test_s12b_worker_companion_e2e.py | create — 11 flows, both cities |

# Implementation Plan

1. Flow 1 — Resume: LLM path on flag → template path when off.
2. Flow 2 — Cover letter: fair-chance vs non-fair-chance, both cities.
3. Flow 3 — PDF export: resume + cover letter, SSRF probe blocked.
4. Flow 4 — Transactional emails: create → confirmation → 24h → 1h.
5. Flow 5 — Signed manage-link: email → click → cancel → replay fail → rotation window (old key valid).
6. Flow 6 — Reminder: 3-barrier stall → exactly 1 SOFT email per 24h.
7. Flow 7 — Plan refresher: HARD stall → refresh → checklist preserved → history capped at 20.
8. Flow 8 — Auto-advance: past → 6h grace → missed → notice → stall suppressed 48h.
9. Flow 9 — Compliance export: request → link → download → archive → link expires.
10. Flow 10 — Selective delete: criminal_record category → appointment still visible, record_profile tombstoned.
11. Flow 11 — Advisor inbox: cross-city 403; demo sessions excluded.
12. Prompt injection assertion; token replay under concurrency; key rotation; uniform token error; k-anonymity; retention sweep.

# Acceptance Criteria

- [ ] All 11 flows pass for both cities
- [ ] Prompt injection: resume generation test with `"Ignore previous instructions"` asserts `generation_method=template`
- [ ] Token replay under concurrency: two threads call manage-URL simultaneously — exactly one succeeds
- [ ] Key rotation: old secret during rotation window still validates kid-tagged tokens
- [ ] Uniform token error: invalid-token vs expired-token vs unknown-appointment-id all return identical 401 body
- [ ] k-anonymity on advisor-facing weekly review: single-session segment suppressed
- [ ] Retention sweep: session past `expires_at + 90d` purged
- [ ] No test pollution; under 90 seconds
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_s12b_worker_companion_e2e.py -v
bpsai-pair arch check backend/tests/test_s12b_worker_companion_e2e.py
```
