---
id: T12.35
title: Integration Gate (S12a)
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 20
status: pending
sprint: 12a
tags:
  - backend
  - gate
  - release
depends_on:
  - T12.26
  - T12.29
  - T12.32
  - T12.33
---

# Objective

S12a final gate. Verifies all foundational infrastructure is solid before S12b builds on it. Full test suite, migration rollback round-trip, scheduler lock under multi-worker, load test of nightly orchestrator, feature-flag defaults audit, table-inventory check, runbook + staging-only constraint documented.

# Files to Update

| File | Change |
|------|--------|
| docs/ops/s12a-rollout.md | create runbook |
| backend/tests/test_s12a_gate.py | create |
| backend/tests/test_s12a_admin_key_scan.py | create |

# Implementation Plan

1. Run full backend + frontend test suites; fix any regressions.
2. Round-trip T12.0 migration: up, seed, down, up; assert integrity.
3. Verify scheduler lock prevents duplicate fires under `WEB_CONCURRENCY=2`.
4. Load test nightly orchestrator with 200 seeded sessions across 2 cities; assert <10min.
5. Verify two-caller pathway hook.
6. Manual browser walk-through: new session → plan → appointments → application → retro → digest.
7. Grep new route files for string literal assignment to variables matching `.*(key|secret|token|password).*` — zero hits required.
8. Assert feature-flag defaults at startup: `ENABLE_AI_GENERATION=false`, `FEATURE_NIGHTLY_ENABLED=true`, `FEATURE_EMAIL_SEND_ENABLED=true`.
9. Assert all 13 tables present after m002.
10. Re-run existing outcome tracker test files; confirm compat.
11. Write runbook at `docs/ops/s12a-rollout.md` covering flag order, SendGrid sender + webhook, 02:00 failure response, migration rollback + staging-only constraint.
12. Document feature flags in PR description.

# Acceptance Criteria

- [ ] Full test suite passes (backend + frontend)
- [ ] Zero regressions in existing 2,100+ backend / 800+ frontend tests
- [ ] All new S12a routes respond correctly on fresh deploy (no `/api/jobs` collision)
- [ ] APScheduler jobs fire on test schedule under `WEB_CONCURRENCY=2` — duplicate fire prevented (tests lock/constraint from T12.3)
- [ ] T12.0 migration rollback round-trip tested on seeded DB: migrate up, seed, migrate down, migrate up — integrity preserved
- [ ] Load test: nightly orchestrator completes <10min with 200 seeded sessions across 2 cities
- [ ] Two-caller pathway hook verified (both `routes/pathway.py` and `routes/plan_intelligence.py` trigger placeholders)
- [ ] Manual browser walk-through: new session → plan → appointments → application → retro → digest
- [ ] `bpsai-pair arch check` passes across all S12a files
- [ ] Broad admin-key scan: grep new route files for any string literal assignment to variables matching `.*(key|secret|token|password).*` — zero hits required
- [ ] Feature-flag defaults audit: `ENABLE_AI_GENERATION=false`, `FEATURE_NIGHTLY_ENABLED=true`, `FEATURE_EMAIL_SEND_ENABLED=true` — asserted at startup
- [ ] All 13 tables present after m002 migration — integrity check against T12.1 inventory
- [ ] OutcomeTracker compat: existing test files (`test_outcome_tracker.py`, `test_outcome_aggregator.py`, `test_s8_coverage_gaps.py`) all pass
- [ ] Runbook at `docs/ops/s12a-rollout.md` documents feature flag enablement order, SendGrid sender verification + webhook setup, 02:00 nightly failure response, and migration rollback procedure (including state-undo for in-flight data)
- [ ] Runbook documents S12a staging-only constraint — no production general availability until S12b T12.36 (data export / right-to-delete) lands
- [ ] PR description documents feature flags required for prod rollout

# Verification

```bash
cd backend && python -m pytest tests/ -x --tb=short
cd frontend && npm test
bpsai-pair arch check backend/app/ backend/scripts/ frontend/src/app/appointments/ frontend/src/app/daily/
```
