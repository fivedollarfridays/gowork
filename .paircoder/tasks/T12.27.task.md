---
id: T12.27
title: Jobs Tracker Page
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P1
complexity: 25
status: pending
sprint: 12b
tags:
  - frontend
  - jobs
depends_on:
  - T12.13
---

# Objective

Create `frontend/src/app/jobs/page.tsx` — kanban view of job applications. Drag library: `@dnd-kit/core` (accessible, tree-shakeable). Per-card: company, role, applied date, resume version + `generation_method` badge. Funnel stats sidebar with conversion rates.

# Files to Update

| File | Change |
|------|--------|
| frontend/src/app/jobs/page.tsx | create — kanban page |
| frontend/src/components/JobKanbanColumn.tsx | create |
| frontend/src/components/JobCard.tsx | create |
| frontend/src/components/FunnelStatsSidebar.tsx | create |
| frontend/package.json | add `@dnd-kit/core` |
| frontend/src/lib/translations/en.json | add jobs kanban strings |
| frontend/src/lib/translations/es.json | add Spanish translations |
| frontend/src/__tests__/jobs/page.test.tsx | create |

# Implementation Plan

1. Install `@dnd-kit/core` and add kanban column + card components.
2. Fetch applications from `/api/job-applications` (S12a T12.13 route).
3. Render all status columns; keyboard-accessible drag via dnd-kit.
4. Per-card render resume version link with `generation_method` badge.
5. Funnel stats sidebar: conversion rates between columns.
6. Drag-to-move triggers status PATCH.
7. All strings in locale files; ESLint check clean.
8. Tests cover render, transitions, stats display.

# Acceptance Criteria

- [ ] Kanban renders with all status columns
- [ ] Drag-to-move triggers status PATCH via dnd-kit (keyboard-accessible)
- [ ] Per-card shows resume version link with `generation_method` badge
- [ ] Funnel stats sidebar with conversion rates
- [ ] All strings in locale files; ESLint check
- [ ] Tests cover kanban render + status transitions + stats display

# Verification

```bash
cd frontend && npm test -- src/__tests__/jobs
cd frontend && npm run lint
```
