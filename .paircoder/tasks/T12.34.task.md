---
id: T12.34
title: Demo Seed Data Extension
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P1
complexity: 15
status: pending
sprint: 12b
tags:
  - backend
  - demo
  - testing
depends_on:
  - T12.12
  - T12.32b
---

# Objective

Extend `backend/app/demo_seed.py` with worker companion data: 5 sessions spanning stall states, across both cities. Demo isolation: all seeded sessions marked with `demo=true` flag (new column in `sessions` via m003) so analytics queries, advisor inboxes, and intelligence aggregates filter them out.

# Files to Update

| File | Change |
|------|--------|
| backend/app/demo_seed.py | extend with 5 worker-companion sessions |
| backend/app/core/migrations/m003_s12b.py | add `sessions.demo` column |
| backend/tests/test_demo_seed_s12b.py | create |

# Implementation Plan

1. Add `demo BOOLEAN DEFAULT FALSE` column to `sessions` via m003.
2. Extend `demo_seed.py` with 5 sessions: one per stall state (none/SOFT/MEDIUM/HARD/breakthrough).
3. Seed appointments, applications, resumes, snapshots for each.
4. Both cities (montgomery, fort-worth) have equivalent seed.
5. Update T12.12 community-funnel query to filter `WHERE demo = FALSE` (regression test).
6. Update T12.31 advisor inbox query to filter `WHERE demo = FALSE`.
7. Verify demo digest preview renders populated for each state.
8. Tests verify seed runs cleanly on fresh DB AND exclusion enforced.

# Acceptance Criteria

- [ ] Seed adds 5 sessions to `--demo` mode
- [ ] All seeded sessions marked `demo=true` — filterable from analytics/advisor/intelligence views
- [ ] All five stall states represented
- [ ] Appointments, applications, resumes, snapshots seeded
- [ ] Both cities have equivalent seed data
- [ ] Demo digest preview renders populated for each state
- [ ] T12.12 community funnel exclusion verified — demo sessions do NOT contribute to aggregates
- [ ] T12.31 advisor inbox exclusion verified — advisor does not see demo sessions as stalled
- [ ] Tests verify seed runs cleanly on fresh DB AND exclusions enforced

# Verification

```bash
cd backend && python -m pytest tests/test_demo_seed_s12b.py -v
bpsai-pair arch check backend/app/demo_seed.py
```
