---
id: T26.7
title: Frontend admin_brightdata API client
plan: plan-2026-05-s26-admin-dashboard
type: feature
priority: P1
complexity: 8
status: pending
sprint: '26'
tags:
- frontend
- sprint-26
- api-client
- brightdata
depends_on:
- T26.4
---

# Objective

Thin typed API client for the BrightData admin trigger endpoints. Same shape as T26.5/T26.6; smallest of the three clients (one trigger + one status function). Wire types match the existing `routes/brightdata.py` Pydantic types 1:1.

# Files

- `frontend/src/lib/api/admin_brightdata.ts` (new)
- `frontend/src/lib/api/__tests__/admin_brightdata.test.ts` (new)

# Acceptance Criteria

- [ ] `frontend/src/lib/api/admin_brightdata.ts`:
  - Imports `fetchWithCookie` + `throwOnApiError` from `_client.ts`
  - Typed wire interfaces: `TriggerCrawlPayload`, `TriggerCrawlResponse`, `CrawlStatus` — match `backend/app/integrations/brightdata/types.py` 1:1
  - `AdminBrightDataApiError extends Error`
  - Functions: `triggerCrawl(payload)`, `getCrawlStatus(snapshotId)` — exact endpoint paths confirmed against T26.4's migrated routes
- [ ] `frontend/src/lib/api/__tests__/admin_brightdata.test.ts` — vitest URL/method/body assertions per function
- [ ] `npx tsc --noEmit` clean
- [ ] vitest suite green

# Verification

- `cd frontend && npx vitest run src/lib/api/__tests__/admin_brightdata.test.ts`
- `cd frontend && npx tsc --noEmit`
- `bpsai-pair task update T26.7 --status done`

# Dependencies

T26.4 — needs the gate-migrated routes (cookie-session, not api-key) so the client's `credentials: "include"` flow works.
