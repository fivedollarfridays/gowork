---
id: T25.7
title: DFW cross-metro summary admin page
plan: plan-2026-05-s25-dallas-expansion
type: feature
priority: P1
complexity: 28
status: done
sprint: '25'
tags:
- admin
- sprint-25
- frontend
- diagnostic
depends_on:
- T25.6
completed_at: '2026-05-08T19:33:30.559420'
---

# Objective

Read-only admin-gated summary page comparing Fort Worth and Dallas side-by-side: resource count by category, employer count + fair-chance percentage, transit route count + total stops, career center count. Backend route reads counts from JSON seed files via `load_city_config(slug).data_dir` — no DB queries, no matching-engine touch. Frontend renders side-by-side cards with a "DFW total" footer row. Page header explicitly states "Read-only diagnostic. Cross-city matching is not enabled." to prevent misinterpretation.

**Charter integrity (load-bearing):** page is display-only. No writes to matching tables. T25.9's grep gate confirms zero Dallas-specific references in `backend/app/modules/matching/`. If a future sprint legitimately needs cross-metro matching, T25.9 fails as the design-review trigger.

# Files

- `backend/app/routes/cities_admin.py` (new — `GET /api/admin/cities/summary`)
- `backend/tests/test_cities_admin.py` (new — admin happy path + 401/403)
- `frontend/src/lib/api/cities_admin.ts` (new — typed API client)
- `frontend/src/app/admin/cities/dfw/page.tsx` (new — side-by-side cards)
- `frontend/src/app/admin/cities/dfw/__tests__/page.test.tsx` (new — vitest)

# Acceptance Criteria

- [x] `backend/app/routes/cities_admin.py` exposes `GET /api/admin/cities/summary` — admin-gated via `require_role("admin")` (S23 pattern); registered in `backend/app/routes/__init__.py:all_routers`
- [x] Response shape: `{cities: [{slug, name, state, resource_counts: {...}, employer_count, fair_chance_count, fair_chance_pct, transit_route_count, transit_stop_count, career_center_count}, ...], dfw_total: {...}}`
- [x] Backend reads counts from JSON seed files via `load_city_config(slug).data_dir`; **no DB queries**, no `select(...)` against any matching table
- [x] `backend/tests/test_cities_admin.py` covers: admin happy path returns 200 with both FW + Dallas summaries; non-admin role returns 403; anonymous returns 401; counts match the seed-file content via direct file-read in the test
- [x] `frontend/src/lib/api/cities_admin.ts` typed API client following S23 assessments client pattern (credentials:include, `_fetchWithTimeout`, single typed `CitiesAdminApiError`)
- [x] `frontend/src/app/admin/cities/dfw/page.tsx` renders side-by-side cards (Fort Worth | Dallas) with a "DFW total" footer row
- [x] Page wrapped in `<RoleGate roles={["admin"]}>` (S23 pattern)
- [x] Page header copy explicitly states: "Read-only diagnostic. Cross-city matching is not enabled."
- [x] `frontend/src/app/admin/cities/dfw/__tests__/page.test.tsx` covers: renders both city cards, footer total = sum, role-gate blocks non-admin (denied state visible), API error renders error card
- [x] `npx tsc --noEmit` clean; `npx next build` green
- [x] No imports from `backend/app/modules/matching/` in either backend or frontend code on this path
- [x] `bpsai-pair arch check` passes on touched files

# Verification

- `cd backend && .venv/bin/pytest tests/test_cities_admin.py -v`
- `cd frontend && npx vitest run src/app/admin/cities/dfw/__tests__/page.test.tsx`
- `cd frontend && npx tsc --noEmit && npx next build`
- `grep -rn "from app.modules.matching" backend/app/routes/cities_admin.py || echo "OK: no matching imports"`
- `grep -rn "modules/matching" frontend/src/app/admin/cities/dfw/ frontend/src/lib/api/cities_admin.ts || echo "OK: no matching imports"`
- `bpsai-pair task update T25.7 --status done`

# Dependencies

T25.6 — validation gate must be green before DFW view ships (else page surfaces stale/incomplete counts).