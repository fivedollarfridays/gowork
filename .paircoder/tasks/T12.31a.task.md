---
id: T12.31a
title: Advisor Auth Model Doc
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P2
complexity: 5
status: done
sprint: 12b
tags:
  - docs
  - security
depends_on: []
---

# Objective

Write `docs/security/advisor-auth.md` documenting advisor identity, city binding, token issuance, rotation, and revocation. Prerequisite to T12.31 endpoint implementation. Document's decision: extend `require_admin_key` with per-advisor tokens + city claim, OR introduce a distinct `AdvisorToken` system.

# Files to Update

| File | Change |
|------|--------|
| docs/security/advisor-auth.md | create |

# Implementation Plan

1. Outline advisor identity model; pick extend-admin vs distinct-token (document tradeoffs).
2. Document token issuance + revocation processes.
3. Describe city-binding: advisor token carries `city` claim; backend enforces on every request.
4. Document rotation policy + incident response steps.
5. Route for security lead review (PR review requirement).

# Acceptance Criteria

- [x] Doc covers: advisor identity model (extend `require_admin_key` with per-advisor tokens + city claim, OR distinct `AdvisorToken` system) — **Option A chosen** (Section 4)
- [x] Token issuance + revocation process documented (Sections 6, 8)
- [x] City binding — advisor token carries `city` claim; backend enforces on every request (Sections 5, 10 contract C2)
- [x] Rotation policy + incident response (Sections 7, 9)
- [ ] Reviewed by security lead (tracked via PR review requirement) — pending PR review

# Verification

```bash
test -f docs/security/advisor-auth.md && wc -l docs/security/advisor-auth.md
```
