---
id: T12.10
title: Appointments API Routes
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 15
status: pending
sprint: 12a
tags:
  - backend
  - routes
  - appointments
depends_on:
  - T12.7
  - T12.9
---

# Objective

Create `backend/app/routes/appointments.py` with nine endpoints: `GET /api/appointments?session_id=X`, `POST /api/appointments`, `GET /api/appointments/{id}`, `PATCH /api/appointments/{id}`, `DELETE /api/appointments/{id}`, `POST /api/appointments/{id}/attended`, `POST /api/appointments/{id}/missed`, `GET /api/appointments/upcoming?session_id=X&days=7`, `POST /api/appointments/from-pathway?session_id=X`.

# Files to Update

| File | Change |
|------|--------|
| backend/app/routes/appointments.py | create — 9 endpoints |
| backend/app/routes/__init__.py | register router |
| backend/tests/test_appointments_routes.py | create |

# Implementation Plan

1. Implement all 9 endpoints with proper HTTP status codes.
2. Use Pydantic for input validation.
3. Add session ownership checks — no cross-session reads.
4. Register in `all_routers`; add a registration test.
5. Write tests for each endpoint covering happy path, auth, malformed input.

# Acceptance Criteria

- [ ] All nine endpoints implemented with proper HTTP codes
- [ ] Input validation via Pydantic
- [ ] Session ownership check (no cross-session reads)
- [ ] Routes registered in `all_routers` and tested for registration
- [ ] Tests cover happy path + auth + malformed input per endpoint
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_appointments_routes.py -v
bpsai-pair arch check backend/app/routes/appointments.py
```
