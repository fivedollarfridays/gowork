---
id: T12.12
title: Jobs Funnel Analytics (with k-anonymity)
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 20
status: pending
sprint: 12a
tags:
  - backend
  - analytics
  - security
depends_on:
  - T12.11
---

# Objective

Elevated from P1 in v2 — T12.33 intelligence wire-up (P0) depends on this. Create `backend/app/modules/jobs/funnel_analytics.py`. Per-session: `compute_funnel(session_id)`. Aggregate (city-scoped): `compute_community_funnel(city, segment_by=None)`. Feeds `outcomes/intelligence.py`. **k-anonymity:** suppress any segment cell with fewer than 5 sessions. Return `null` count + `suppressed: true` flag.

# Files to Update

| File | Change |
|------|--------|
| backend/app/modules/jobs/funnel_analytics.py | create — per-session + community funnels |
| backend/app/modules/outcomes/intelligence.py | expose application_conversion_rates |
| backend/tests/test_funnel_analytics.py | create |

# Implementation Plan

1. Implement `compute_funnel(session_id)` returning draft/applied/interview/offer counts + conversion rates.
2. Implement `compute_community_funnel(city, segment_by=None)` segmented by `cleared_barriers`, `fair_chance_employer`, `industry`.
3. Apply k-anonymity: cells with < 5 sessions return `null` + `suppressed: true`.
4. Wire `application_conversion_rates` into `/api/intelligence/barriers` response — additive only, no shape break.
5. Exclude demo-tagged sessions (`demo=true` from S12b T12.34) from aggregates; add guard now.
6. Write tests: empty DB, single session, single-cell suppression, multi-session above threshold.

# Acceptance Criteria

- [ ] Per-session funnel returns structured dict (draft/applied/interview/offer counts + conversion rates)
- [ ] Community funnel segments correctly by `cleared_barriers`, `fair_chance_employer`, `industry`
- [ ] Cells with < 5 sessions return `null` with `suppressed: true` — not the count
- [ ] `outcomes/intelligence.py` imports + exposes `application_conversion_rates` in `/api/intelligence/barriers` response — additive only, no shape break
- [ ] Demo sessions (tagged `demo=true` — S12b T12.34) excluded from aggregates (guard added now so S12b seed doesn't leak)
- [ ] Tests cover empty DB, single session, single-cell suppression, multi-session above threshold
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_funnel_analytics.py -v
bpsai-pair arch check backend/app/modules/jobs/funnel_analytics.py
```
