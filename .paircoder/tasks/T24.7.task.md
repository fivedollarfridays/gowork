---
id: T24.7
title: Reputation event recording API
plan: plan-2026-05-s24-listing-verification
type: feature
priority: P0
complexity: 15
status: done
sprint: '24'
tags:
- verification
- sprint-24
- reputation
- events
depends_on:
- T24.2
completed_at: '2026-05-08T08:47:59.332491'
---

# Objective

`POST /api/listings/{listing_id}/events {kind, session_id?, notes?}` records a reputation signal. Gated by `any_of_roles("case_manager", "admin")` — case managers record placements they witness; admins record manual events. Anonymous session_id permitted in payload so events tied to anonymous candidates aren't lost. Persists to listing_reputation_events with recorded_by = requesting account.

Lands in NEW route module — must NOT extend `auth.py`.

# Files

- `backend/app/routes/listing_reputation.py` (new)
- `backend/app/routes/__init__.py` (register router)
- `backend/tests/test_listing_reputation.py` (new)
- `backend/tests/_audit_integrity_fixtures.py` (extend AUDIT_ALLOWLIST)
- `backend/tests/_cross_session_fixtures.py` (extend PUBLIC_ENDPOINTS)

# Acceptance Criteria

- [x] `POST /api/listings/{listing_id}/events` route
- [x] Body: `{kind: str, session_id: str | None, notes: str | None}`; kind validated against EVENT_KINDS tuple
- [x] Gated: any_of_roles("case_manager", "admin")
- [x] Stores in listing_reputation_events; recorded_by = requesting account id (resolved from gw_account cookie)
- [x] 404 if listing_id not in job_listings
- [x] Router registered in all_routers
- [x] AUDIT_ALLOWLIST + PUBLIC_ENDPOINTS extended
- [x] Tests cover: each event_kind, role gating (case_manager OK, admin OK, sme_reviewer 403, anonymous 403), unknown listing 404, invalid kind 422, anonymous session_id payload supported
- [x] bpsai-pair arch check passes

# Verification

- `cd backend && .venv/bin/python -m pytest tests/test_listing_reputation.py -v`
- `wc -l backend/app/routes/auth.py` — 314
- `bpsai-pair arch check backend/app/routes/listing_reputation.py`
- `bpsai-pair task update T24.7 --status done`

# Dependencies

- T24.2 — needs queries_listings_reputation.record_event.