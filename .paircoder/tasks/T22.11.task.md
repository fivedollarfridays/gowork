---
id: T22.11
title: Account-aware client + session-claim CTAs
plan: plan-2026-05-s22-identity-foundation
type: feature
priority: P0
complexity: 15
status: done
sprint: '22'
tags:
- identity
- sprint-22
- frontend
- cta
depends_on:
- T22.10
completed_at: '2026-05-06T18:16:58.504796'
---

# Objective

Extend the React Query client with a `useAccount()` hook surfacing account state. Add `<SaveProgressCTA />` component rendered at strategic points in the assessment funnel — post-assessment, post-plan-generation, and pre-share. The CTA is opt-in only; it never gates functionality.

# Files

- `frontend/src/lib/api/client.ts` (extend)
- `frontend/src/components/auth/SaveProgressCTA.tsx` (new)
- `frontend/src/app/plan/page.tsx` (insertion)
- `frontend/src/app/assess/page.tsx` (insertion)
- `frontend/src/app/shared/[token]/page.tsx` (insertion)
- `frontend/src/__tests__/auth/save-progress-cta.test.tsx` (new)

# Acceptance Criteria

- [x] `frontend/src/lib/api/client.ts` exposes `useAccount()` returning `{accountId, email} | null`
- [x] `frontend/src/components/auth/SaveProgressCTA.tsx`: dismissible card with "Save your progress" headline and email-form action
- [x] Inserted at: end of `/assess` (post-assessment), middle of `/plan` (post-plan-generation), and on `/shared/[token]` page header (pre-share)
- [x] Dismissed CTA does not re-show in the same session (localStorage-backed)
- [x] Already-claimed sessions never see the CTA
- [x] vitest coverage for the component + insertion points
- [x] No build regressions

# Verification

- `cd frontend && npx vitest run src/__tests__/auth/save-progress-cta.test.tsx`
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npx next build`
- Manual: complete assessment as anonymous → CTA visible; dismiss → no re-show; claim session → CTA hidden
- `bpsai-pair task update T22.11 --status done`

# Dependencies

- T22.10 — needs the auth API client and login pages to drive the CTA action.