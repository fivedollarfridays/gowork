---
id: T22.1
title: Alembic infrastructure + async env
plan: plan-2026-05-s22-identity-foundation
type: feature
priority: P0
complexity: 15
status: done
sprint: '22'
tags:
- identity
- sprint-22
- migration
- alembic
depends_on: []
completed_at: '2026-05-06T16:42:27.353918'
---

# Objective

Set up Alembic at `backend/alembic/` with async-engine support. Establish the migration runner that will replace the hand-rolled `m001`-`m010` chain. No schema changes in this task — purely the runner. Migration ordering convention: numeric (`0001_`, `0002_`, ...) to mirror the existing `m00X` shape.

# Files

- `backend/alembic.ini` (new)
- `backend/alembic/env.py` (new)
- `backend/alembic/script.py.mako` (new)
- `backend/alembic/versions/.gitkeep` (new)

# Acceptance Criteria

- [x] `backend/alembic.ini` configured with `script_location = alembic/`
- [x] `backend/alembic/env.py` supports both sqlite and postgres via DATABASE_URL
- [x] `backend/alembic/env.py` uses async engine (matches existing `aiosqlite` pattern)
- [x] `backend/alembic/script.py.mako` template uses `0001_`-style filenames
- [x] `backend/alembic/versions/.gitkeep` committed (empty until T22.3)
- [x] `alembic upgrade head` runs clean against fresh empty DB on both engines
- [x] `bpsai-pair arch check backend/alembic/env.py` passes

# Verification

- `cd backend && alembic upgrade head` against fresh sqlite DB
- `cd backend && DATABASE_URL=postgresql+asyncpg://... alembic upgrade head` against fresh postgres DB
- `bpsai-pair arch check backend/alembic/env.py`
- `bpsai-pair task update T22.1 --status done`

# Dependencies

None — sprint entry task.