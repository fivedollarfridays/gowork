---
id: T12.5
title: Shared Schemas + Timezone Helpers
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 15
status: in_progress
sprint: 12a
tags:
- backend
- types
- foundation
depends_on: []
---

# Objective

Create `backend/app/modules/common/temporal_types.py` with Pydantic models and enums shared across appointments/jobs/engagement: `AppointmentType`, `AppointmentStatus`, `JobApplicationStatus`, `EngagementEventType`, `StallLevel`, `GenerationMethod`. Port `ops:lib/nightly_phases/timezone_utils.py:format_ct` as city-generic `format_city_local(dt, city)`.

# Files to Update

| File | Change |
|------|--------|
| backend/app/modules/common/__init__.py | create package |
| backend/app/modules/common/temporal_types.py | create — shared enums + models |
| backend/tests/test_temporal_types.py | create |

# Implementation Plan

1. Define `AppointmentType`, `AppointmentStatus`, `JobApplicationStatus`, `EngagementEventType`, `StallLevel`, `GenerationMethod` as stable string enums.
2. Ensure round-trippable through SQLite TEXT (verify with a test).
3. Define Pydantic models that validate timezone awareness.
4. Implement `format_city_local(dt, city) -> str` using the city's configured timezone.
5. Confirm no circular imports with existing modules.
6. Write tests for enum membership, model validation, formatter across two cities (Montgomery + Fort Worth).

# Acceptance Criteria

- [ ] All shared enums defined with stable string values (tested round-trippable through SQLite TEXT)
- [ ] `GenerationMethod = Enum("llm" | "template")` present (used by S12b)
- [ ] Pydantic models validate timezone awareness
- [ ] `format_city_local(dt, city) -> str` renders in the city's configured timezone
- [ ] No circular imports with existing modules
- [ ] Tests cover enum membership, model validation, formatter across two cities
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_temporal_types.py -v
bpsai-pair arch check backend/app/modules/common/temporal_types.py
```