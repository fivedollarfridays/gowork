---
id: T12.22a
title: Weekly Review Composer
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P1
complexity: 20
status: pending
sprint: 12b
tags:
  - backend
  - engagement
  - analytics
depends_on:
  - T12.23
  - T12.25
---

# Objective

Port `ops:lib/nightly_review.py` (`review_applications`, `review_engagement`, `build_review`) + `ops:lib/daily_plan_summary.py` (`aggregate_summaries`, `format_summary_report`) to `backend/app/modules/plan/weekly_review.py`. Runs Sunday only — T12.25 orchestrator gains a day-of-week branch. k-anonymity on advisor-facing aggregates (same 5-session suppression as T12.12 community funnel). Digest-open counts sourced from SendGrid `open` events captured by T12.2a.

# Files to Update

| File | Change |
|------|--------|
| backend/app/modules/plan/weekly_review.py | create — weekly composer |
| backend/app/modules/orchestrator/nightly.py | add Sunday branch |
| backend/tests/test_weekly_review.py | create |

# Implementation Plan

1. Port `review_applications`, `review_engagement`, `build_review`; return `WeeklyReview` struct.
2. Port `aggregate_summaries`, `format_summary_report` for markdown rendering.
3. Wire digest-open count off `sendgrid_events` `open` rows (from S12a T12.2a webhook).
4. Compute funnel-style job-app progression over the 7-day window.
5. Summarize barriers cleared in the week.
6. Apply k-anonymity suppression (<5 sessions → suppressed flag) on advisor-facing sections.
7. Update T12.25 orchestrator: Sunday runs invoke `build_weekly_review` alongside daily retro.
8. Tests cover empty week, active week, mixed-signal week, Sunday branch, suppression.

# Acceptance Criteria

- [ ] `build_weekly_review(session_id, date_range)` returns `WeeklyReview` struct
- [ ] Funnel-style review of job app progression over window
- [ ] Engagement review sourced from `sendgrid_events` `open` rows (S12a T12.2a webhook)
- [ ] Barriers-cleared summary across week
- [ ] `format_summary_report(review)` renders human-readable markdown
- [ ] k-anonymity suppression on any advisor-facing aggregate sections — cells <5 sessions return suppressed flag
- [ ] T12.25 orchestrator updated: Sunday runs invoke `build_weekly_review` in addition to daily retro
- [ ] Tests cover empty week, active week, mixed-signal week, Sunday branch, suppression on small-n
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_weekly_review.py -v
bpsai-pair arch check backend/app/modules/plan/weekly_review.py
```
