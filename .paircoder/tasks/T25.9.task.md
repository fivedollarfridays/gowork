---
id: T25.9
title: Sprint 25 integration gate
plan: plan-2026-05-s25-dallas-expansion
type: feature
priority: P0
complexity: 10
status: pending
sprint: '25'
tags:
- integration
- sprint-25
- gate
- charter
depends_on:
- T25.6
- T25.7
- T25.8
---

# Objective

Final gate. Re-runs all suites on both sqlite + postgres axes. Asserts the load-bearing charter integrity invariant: matching engine reads ZERO Dallas-specific signals (carrying forward S24's grep pattern). Reconciles state.md (demote S22-S24 to Previous Sprints summary; refresh Current Focus to Dallas; reset What's Next). Reconciles ROADMAP.md (check off Dallas Expansion phase items; add S25 to Completed). Pushes PR; CI green on all jobs.

# Files

- `backend/tests/test_charter_integrity_dallas.py` (new — grep assertion)
- `.paircoder/context/state.md` (reconcile)
- `ROADMAP.md` (reconcile)
- PR description (cross-link to brief + backlog)

# Acceptance Criteria

- [ ] Full backend pytest suite green on sqlite + postgres axes (no regressions vs S24 baseline 4700 passed / 4 baseline failed / 2 skipped)
- [ ] Frontend vitest suite green (no regressions vs S24 baseline 3620 passed / 3 skipped / 0 failed)
- [ ] `ruff check .` clean on touched files
- [ ] `bpsai-pair arch check .` no new violations
- [ ] **CHARTER INTEGRITY ASSERTION:** subprocess grep across `backend/app/modules/matching/` for any reference to `dallas`, `DART`, `Dallas`, `75201`, `75398`, or any Dallas-specific resource/employer name returns ZERO matches. The matching engine remains city-symmetric; no metro-special-case logic. Test file is `backend/tests/test_charter_integrity_dallas.py`. Test fails as design-review trigger if a future sprint legitimately needs cross-metro matching.
- [ ] `tsc --noEmit` 0 errors; `next build` green
- [ ] Postgres CI service container exercises Dallas-aware code paths; no new alembic migrations in S25 (alembic chain stays at head 0014 from S24)
- [ ] `.paircoder/context/state.md` reconciled: Sprint 25 complete entry under "What Was Just Done"; Current Focus updated to summarize Dallas + DFW summary delivery; S22-S24 demoted to Previous Sprints summary; What's Next reset
- [ ] `ROADMAP.md` reconciled: Dallas Expansion phase items checked off; new entry added under "Completed" → "Dallas Expansion (Sprint S25) — merged YYYY-MM-DD (PR #N)"
- [ ] PR pushed; CI green on all jobs

# Verification

- `cd backend && .venv/bin/pytest tests/test_charter_integrity_dallas.py -v`
- `cd backend && .venv/bin/pytest -q 2>&1 | tail -5` (full suite)
- `cd frontend && npx vitest run 2>&1 | tail -5` (full suite)
- `cd backend && .venv/bin/ruff check .`
- `bpsai-pair arch check .`
- `cd frontend && npx tsc --noEmit && npx next build 2>&1 | tail -3`
- `gh pr view --json statusCheckRollup -q '.statusCheckRollup[].conclusion' | sort -u`
- `bpsai-pair task update T25.9 --status done`
- `bpsai-pair plan complete plan-2026-05-s25-dallas-expansion`

# Dependencies

- T25.6 — validation gate green
- T25.7 — DFW summary page ships (P1; if descoped, gate still runs)
- T25.8 — frontend plumbing ships (P1; if descoped, gate still runs)
