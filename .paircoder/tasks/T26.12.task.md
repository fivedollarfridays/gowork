---
id: T26.12
title: Sprint 26 integration gate
plan: plan-2026-05-s26-admin-dashboard
type: feature
priority: P0
complexity: 10
status: pending
sprint: '26'
tags:
- integration
- sprint-26
- gate
- charter
depends_on:
- T26.2
- T26.8
- T26.9
- T26.10
- T26.11
---

# Objective

Final gate. Re-runs all suites on both sqlite + postgres axes. Re-asserts every charter invariant carried forward from prior sprints. Reconciles state.md + ROADMAP.md. Pushes PR; CI green on all 5 jobs.

The cross-session allowlist registration was a per-route AC on every backend task this sprint (S25 lesson learned — T25.7's CI-only failure was preventable). This gate verifies it actually happened by running the contract test.

# Files

- `backend/tests/_cross_session_fixtures.py` (verify all S26 admin routes registered — extend if any missed)
- `.paircoder/context/state.md` (reconcile)
- `ROADMAP.md` (reconcile)
- PR description (cross-link to brief + backlog)

# Acceptance Criteria

- [ ] Full backend pytest suite green on sqlite + postgres axes (no regressions vs S25 baseline 4824 passed / 4 baseline failed / 2 skipped)
- [ ] Frontend vitest suite green (no regressions vs S25 baseline 3629 passed / 3 skipped)
- [ ] `ruff check .` clean on touched files
- [ ] `bpsai-pair arch check .` no new violations
- [ ] **Cross-session isolation contract holds:** `tests/test_cross_session_isolation.py::test_every_endpoint_is_either_flagged_or_allowlisted` re-runs green. Every new admin endpoint shipped in S26 (T26.2's 6 routes + T26.3's 5 routes + T26.4's brightdata routes) is in `_cross_session_fixtures.py:PUBLIC_ENDPOINTS` with rationale.
- [ ] **Charter integrity assertion (S25 carryforward):** `backend/tests/test_charter_integrity_dallas.py` re-runs green (admin dashboard adds zero matching-engine references)
- [ ] **Anonymous-first invariant carryforward (S22):** `backend/tests/test_anonymous_first_invariant.py` re-runs green; zero new auth-gated USER routes (admin routes are operator-only and exempt by design)
- [ ] `tsc --noEmit` 0 errors; `next build` green
- [ ] Postgres axis green (S22 dual-engine + S23 isolation rebuild)
- [ ] `.paircoder/context/state.md` reconciled: Sprint 26 complete entry under "What Was Just Done"; Current Focus updated; S25 demoted to Previous Sprints summary; What's Next reset for S27
- [ ] `ROADMAP.md` reconciled: "Phase: Admin Dashboard" items checked off under Planned Next; new entry added under Completed → "Admin Dashboard (Sprint S26) — merged YYYY-MM-DD (PR #N)"
- [ ] PR pushed; CI green on all 5 jobs (Backend Python, Backend Postgres, Frontend, Security Checks, Lighthouse CI)

# Verification

- `cd backend && .venv/bin/pytest -q 2>&1 | tail -5`  (full suite)
- `cd frontend && npx vitest run 2>&1 | tail -5`  (full suite)
- `cd backend && .venv/bin/pytest tests/test_cross_session_isolation.py tests/test_charter_integrity_dallas.py tests/test_anonymous_first_invariant.py -v`  (charter + invariants)
- `bpsai-pair arch check .`
- `cd frontend && npx tsc --noEmit && npx next build 2>&1 | tail -3`
- `gh pr checks <PR_NUMBER>`  (verify CI green)
- `bpsai-pair task update T26.12 --status done`
- `bpsai-pair plan complete plan-2026-05-s26-admin-dashboard`

# Dependencies

- T26.2 — backend CRUD + router registrations must land
- T26.8 — `/admin/resources` page
- T26.9 — `/admin/feedback` page
- T26.10 — `/admin/brightdata` page (P1; if descoped, gate still runs)
- T26.11 — `/admin` landing page (P1; if descoped, gate still runs)
