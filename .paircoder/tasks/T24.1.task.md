---
id: T24.1
title: 'Alembic migration: employer_accounts + listing_claims + listing_verifications + listing_reputation_events'
plan: plan-2026-05-s24-listing-verification
type: feature
priority: P0
complexity: 25
status: pending
sprint: '24'
tags: [verification, sprint-24, schema, migration]
depends_on: []
---

# Objective

Add the four-table verification substrate. Alembic revision 0014 with `down_revision='0013'`. Uses SQLAlchemy Core MetaData mirroring S22+S23 pattern — shared `accounts_schema.metadata` so FKs resolve at create_all time. ENUM constraints via portable CHECK clauses sourced from module-level tuples.

The four tables are parallel to (not a replacement for) the existing `job_listings` and `employer_policies` tables — verification metadata lives separately so this sprint does not churn the matching engine's substrate.

# Files

- `backend/alembic/versions/0014_listing_verification.py` (new)
- `backend/app/core/listings_verification_schema.py` (new)
- `backend/tests/test_listings_verification_schema.py` (new)

# Acceptance Criteria

- [ ] Revision 0014, down_revision 0013
- [ ] Four `Table` defs on shared `accounts_schema.metadata`
- [ ] `employer_accounts(id PK, name UNIQUE, domain, verification_status, verified_at NULL, verified_by_account_id FK accounts NULL, retired_at NULL, source_trust_tier, created_at)`
- [ ] `listing_claims(id PK, claim_token_hash UNIQUE, listing_id FK job_listings, employer_account_id FK NULL, claimant_email, claimant_account_id FK accounts NULL, expires_at, used_at NULL, created_at)`
- [ ] `listing_verifications(listing_id FK job_listings UNIQUE, employer_account_id FK, verification_tier, intake_completed_at NULL, intake_json TEXT NULL, verified_at, created_at)` — UNIQUE on listing_id
- [ ] `listing_reputation_events(id PK, listing_id FK, event_kind, session_id NULL, occurred_at, recorded_by FK accounts NULL, notes TEXT NULL)`
- [ ] ENUM CHECK constraints: `verification_status` (pending|claimed|admin_review|verified|retired), `verification_tier` (source_trust|claim_verified|admin_reviewed), `event_kind` (response_received|withdrawn|placed|ghosted), `source_trust_tier` (unknown|brightdata|honestjobs|twc|manual)
- [ ] Module-level tuples: `VERIFICATION_STATUSES`, `VERIFICATION_TIERS`, `EVENT_KINDS`, `SOURCE_TRUST_TIERS`
- [ ] `apply_ddl(connection)` helper scoped to the four tables
- [ ] Index on `(listing_id, occurred_at)` on `listing_reputation_events`
- [ ] alembic upgrade head clean on sqlite (postgres deferred to T24.11 CI)
- [ ] bpsai-pair arch check passes on touched files

# Verification

- `cd backend && rm -f /tmp/_t24_test.db && DATABASE_URL=sqlite+aiosqlite:////tmp/_t24_test.db .venv/bin/alembic upgrade head`
- `sqlite3 /tmp/_t24_test.db ".schema employer_accounts listing_claims listing_verifications listing_reputation_events"`
- `bpsai-pair arch check backend/app/core/listings_verification_schema.py`
- `bpsai-pair task update T24.1 --status done`

# Dependencies

None — sprint entry task.
