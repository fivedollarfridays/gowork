---
id: T25.8
title: Frontend Dallas plumbing + i18n
plan: plan-2026-05-s25-dallas-expansion
type: feature
priority: P1
complexity: 15
status: pending
sprint: '25'
tags:
- frontend
- sprint-25
- i18n
depends_on:
- T25.1
---

# Objective

Wire Dallas into the frontend demo-mode hooks and translation catalogs. Add Dallas to `DEMO_ZIPS`. Parametrize the existing city-aware demo-mode test over both fort-worth and dallas. Add Dallas display-name translations to en.json + es.json. **NO new selector UI** — ZIP→city resolution stays canonical (anonymous-first invariant).

The minimum-viable cut (if budget tightens): add `DEMO_ZIPS["dallas"]` only, defer i18n catalog parity to S26.

# Files

- `frontend/src/hooks/useDemoMode.ts` (extend `DEMO_ZIPS`)
- `frontend/src/hooks/__tests__/useDemoMode-cityaware.test.ts` (extend — parametrize over both cities)
- `frontend/src/lib/translations/en.json` (extend — Dallas display strings)
- `frontend/src/lib/translations/es.json` (extend — Dallas display strings)

# Acceptance Criteria

- [ ] `frontend/src/hooks/useDemoMode.ts` adds `DEMO_ZIPS["dallas"] = "75201"` (canonical Dallas demo ZIP)
- [ ] `frontend/src/hooks/__tests__/useDemoMode-cityaware.test.ts` parametrized over `(city, expectedZip)` for both `fort-worth` and `dallas`; both pass
- [ ] `frontend/src/lib/translations/en.json` adds Dallas display strings (city name, region label) — keep symmetric with FW entries
- [ ] `frontend/src/lib/translations/es.json` adds matching Spanish translations
- [ ] No selector UI added; no new component file under `frontend/src/components/` for city picking
- [ ] Translation key audit (existing `missingKeysAudit` in vitest) passes — both EN + ES catalogs have equal Dallas-key coverage
- [ ] `npx tsc --noEmit` clean; `npx next build` green
- [ ] vitest suite green vs S24 baseline (3620 passed / 3 skipped / 0 failed)

# Verification

- `cd frontend && npx vitest run src/hooks/__tests__/useDemoMode-cityaware.test.ts`
- `cd frontend && npx vitest run -t missingKeysAudit`
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npx next build 2>&1 | tail -5`
- `bpsai-pair task update T25.8 --status done`

# Dependencies

T25.1 — needs `cities/dallas.yaml` to exist so the demo-mode contract (city slug → ZIP) is grounded against a real city config.
