---
id: T13.1
title: QC Config + Suite Directory + Authoring Template
plan: plan-2026-04-s13-platform-qc
type: test
priority: P0
complexity: 20
status: done
sprint: '13'
tags:
- qc
- browser-qc
- backend
- frontend
- submission
completed_at: '2026-04-24T22:52:52.593095'
---

# Objective

Create `.paircoder/qc/config.yaml` with environment profiles (`dev` → localhost; `staging` placeholder). Create `.paircoder/qc/suites/` directory. Write `.paircoder/qc/suites/_template.qc.yaml` documenting the schema (name, environment, setup, steps, assertions, cleanup). Write `.paircoder/qc/suites/README.md` with authoring conventions (naming, seeded data requirements, cleanup contract, how to run via divona).

# Acceptance Criteria

- [x] `.paircoder/qc/config.yaml` exists with at least `dev` profile pointing at `http://localhost:3000` (frontend) and `http://localhost:8000` (backend)
- [x] `.paircoder/qc/suites/_template.qc.yaml` documents every required field with comments
- [x] `.paircoder/qc/suites/README.md` covers: suite naming, demo-session requirements, cleanup contract, how to invoke via divona, what a "step" vs "assertion" is
- [x] Divona agent can load the template without error (smoke run)
- [x] `bpsai-pair arch check` passes