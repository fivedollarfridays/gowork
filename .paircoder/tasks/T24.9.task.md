---
id: T24.9
title: Admin claim-review dashboard
plan: plan-2026-05-s24-listing-verification
type: feature
priority: P0
complexity: 25
status: done
sprint: '24'
tags:
- verification
- sprint-24
- frontend
- admin
depends_on:
- T24.5
completed_at: '2026-05-08T09:39:35.174736'
---

# Objective

Two pages under `/admin/listings`: list of pending claims that landed in admin_review tier (domain mismatch from T24.3), and a detail page showing claim email + listing details + employer_account candidate + intake (if filled) + approve/reject buttons. Approve transitions verification_tier to admin_reviewed and creates the verification record; reject deletes the claim. Wrapped in `<RoleGate roles={["admin"]}>` from S23.

# Files

- `frontend/src/app/admin/listings/page.tsx` (new — claim queue list)
- `frontend/src/app/admin/listings/[claimId]/page.tsx` (new — claim detail + approve/reject)
- `frontend/src/lib/api/listing_claims.ts` (new — typed client)
- `backend/app/routes/employers.py` (extend with admin approve/reject endpoints)
- `frontend/src/__tests__/admin/listings/list.test.tsx` (new)
- `frontend/src/__tests__/admin/listings/detail.test.tsx` (new)
- `frontend/src/__tests__/admin/listings/api.test.ts` (new)

# Acceptance Criteria

- [x] List page: fetches via listPendingClaims(); filter by source/age; row click → detail
- [x] Detail page: shows claim email, listing details, employer candidate, intake (if filled), approve/reject buttons
- [x] Approve calls POST /api/employers/claims/{claim_id}/approve (admin-gated; new backend route)
- [x] Reject calls DELETE /api/employers/claims/{claim_id} (admin-gated)
- [x] Backend routes added to existing employers.py module; gated by require_role("admin")
- [x] Frontend pages render under /admin/* layout's existing RoleGate; detail page additionally gates the approve/reject buttons on useAccountRoles().includes("admin")
- [x] Typed client functions: listPendingClaims, getClaim, approveClaim, rejectClaim
- [x] Approve cyan (bg-primary), reject rose (bg-destructive)
- [x] vitest coverage for both pages + API client (request/response shapes + error states)
- [x] npx tsc --noEmit clean; npx next build green

# Verification

- `cd frontend && npx vitest run src/__tests__/admin/listings/`
- `cd frontend && npx tsc --noEmit && npx next build`
- `wc -l backend/app/routes/auth.py` — 314
- `bpsai-pair task update T24.9 --status done`

# Dependencies

- T24.5 — admin dashboard reads the intake state to show review queue (intake submission must be possible before this dashboard surfaces anything meaningful).