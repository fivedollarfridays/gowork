---
id: T25.3
title: Dallas employer index (fair-chance + general)
plan: plan-2026-05-s25-dallas-expansion
type: feature
priority: P0
complexity: 30
status: pending
sprint: '25'
tags:
- seed-data
- sprint-25
- employers
- fair-chance
depends_on:
- T25.1
---

# Objective

Author `data/cities/dallas/employers.json`, `data/cities/dallas/employer_policies_seed.json`, and `backend/data/cities/dallas/honestjobs_listings.json` with curated Dallas-area employer entries from open sources (DFW Hire Reentry, Workforce Solutions Greater Dallas employer directory, public fair-chance pledge lists). Schema parity with FW. Append `honestjobs` to `cities/dallas.yaml` `job_adapters` list once the seed file lands so the `JobAggregator` path picks it up.

The dual-path (matcher + aggregator) is intentional: `data/cities/dallas/employers.json` feeds the matching engine; `backend/data/cities/dallas/honestjobs_listings.json` feeds the JobAggregator's honestjobs adapter. Both must ship together — adding `honestjobs` to the yaml without the listings file would surface as an empty-aggregator regression.

# Files

- `data/cities/dallas/employers.json` (new)
- `data/cities/dallas/employer_policies_seed.json` (new)
- `backend/data/cities/dallas/honestjobs_listings.json` (new)
- `cities/dallas.yaml` (extend `job_adapters`)
- `backend/tests/test_dallas_seed_employers.py` (new — schema + duplication check)

# Acceptance Criteria

- [ ] `data/cities/dallas/employers.json` includes ≥30 Dallas-area employers from open sources, mixed reentry-friendly + general
- [ ] `data/cities/dallas/employer_policies_seed.json` mirrors FW schema (background-check timing, fair-chance flags, application steps)
- [ ] `backend/data/cities/dallas/honestjobs_listings.json` includes ≥15 Dallas-specific listings (FW honestjobs entries already tagged "DFW" remain in the FW seed; do not duplicate)
- [ ] `cities/dallas.yaml` `job_adapters` list updated to add `honestjobs` after seed lands
- [ ] All employer entries include `name`, `category`, `lat`, `lng`, `address`, `phone`, `url`, `fair_chance: bool`, `background_check_timing` (matching FW employer_policies fields)
- [ ] No duplication with FW seed: cross-file `name`+`address` intersection between `data/cities/fort-worth/employers.json` and `data/cities/dallas/employers.json` is empty (or each duplicate is explicitly tagged with a `metro: DFW` shared marker)
- [ ] `JobAggregator` smoke test: `get_city_config('dallas').job_adapters` includes `honestjobs`; aggregator resolves the listings file via the city `data_dir` path
- [ ] JSON parses clean, UTF-8 encoded
- [ ] Schema parity test (`test_dallas_seed_employers.py`) — every Dallas employer entry has the same key set as FW employer entries

# Verification

- `cd backend && .venv/bin/python -c "import json; data = json.load(open('../data/cities/dallas/employers.json')); print(f'{len(data)} employers')"`
- `cd backend && .venv/bin/python -c "import yaml; cfg = yaml.safe_load(open('../cities/dallas.yaml')); assert 'honestjobs' in cfg['job_adapters'], cfg['job_adapters']; print('honestjobs wired:', cfg['job_adapters'])"`
- `cd backend && .venv/bin/pytest tests/test_dallas_seed_employers.py -v`
- `bpsai-pair task update T25.3 --status done`

# Dependencies

T25.1 — needs `cities/dallas.yaml` to extend `job_adapters` and `data_dir` for resolution.
