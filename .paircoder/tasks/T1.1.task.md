---
id: T1.1
title: CityConfig schema + YAML loader
plan: plan-sprint-1-engage
type: feature
priority: P0
complexity: 25
status: done
sprint: '1'
depends_on: []
completed_at: '2026-04-11T15:28:09.324960'
---

# CityConfig schema + YAML loader

Define a Pydantic `CityConfig` model and a `load_city_config(city: str) -> CityConfig` loader that reads `cities/{city}.yaml`. Add a `CITY: str = "montgomery"` setting to `backend/app/core/config.py` and expose a `get_city_config()` helper that uses the active `CITY` env var. This task creates the new `backend/app/cities/` package.

# Acceptance Criteria

- [x] `CityConfig` model validates required fields: `name`, `state`, `zip_ranges`, `job_adapters` (list of adapter names), `data_dir`
- [x] `load_city_config("montgomery")` loads `cities/montgomery.yaml` and returns a populated `CityConfig`
- [x] `load_city_config("fort-worth")` loads the scaffold `cities/fort-worth.yaml` without error
- [x] `load_city_config("nonexistent")` raises `CityConfigNotFoundError` with a clear message
- [x] `CITY=montgomery` is the default; existing env-var parsing unchanged
- [x] `bpsai-pair arch check backend/app/cities/config.py` passes
- [x] `bpsai-pair arch check backend/app/core/config.py` still passes after modification