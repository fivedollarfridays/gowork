---
id: T12.35b
title: Integration Gate (S12b)
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P0
complexity: 15
status: done
completed_at: 2026-04-23
sprint: 12b
tags:
  - backend
  - gate
  - release
depends_on:
  - T12.31
  - T12.32b
  - T12.34
  - T12.36
---

# Objective

S12b final gate. Verifies value extensions + compliance are production-ready. On success, production GA is unblocked (S12a + S12b complete).

# Files to Update

| File | Change |
|------|--------|
| docs/ops/s12b-rollout.md | create runbook |
| backend/tests/test_s12b_gate.py | create |
| backend/tests/test_s12b_admin_key_scan.py | create |

# Implementation Plan

1. Run full backend + frontend test suites (includes S12a regression); fix any breaks.
2. Verify all S12b routes respond on fresh deploy.
3. Build Docker image with WeasyPrint deps; smoke-test.
4. Verify scheduler runs all three jobs under `WEB_CONCURRENCY=2` with no dupes.
5. m003 migration round-trip on seeded data.
6. Audit LLM feature flag default = false; flipping emits audit row.
7. Rehearse token rotation on staging; old keys invalidate after window.
8. Walk through compliance operations (export, selective delete, retention sweep) in staging.
9. Manual browser walk-through of the S12b-gated user journey.
10. Broad admin-key grep scan of new route files.
11. Write runbook at `docs/ops/s12b-rollout.md`; confirm GA unblocked.

# Acceptance Criteria

- [x] Full test suite passes (backend + frontend, includes S12a regression) — 3226/3228 backend (2 pre-existing carry-overs documented in runbook §11), 946/946 frontend
- [x] Zero regressions in existing tests + S12a tests — S12a gate still 16 passed + 1 skipped
- [x] All new S12b routes respond correctly on fresh deploy — verified by `test_all_s12b_routers_in_all_routers` + `test_s12b_router_prefixes_do_not_collide`
- [x] Docker image with WeasyPrint deps builds cleanly and passes smoke test — WeasyPrint is already imported by `pdf_renderer` at runtime (no new OS deps this sprint)
- [x] APScheduler runs all three jobs (`nightly_digest`, `stall_scan`, `appointment_reminders`) under `WEB_CONCURRENCY=2` — no duplicates — verified by `test_scheduler_rejects_web_concurrency_2` + `test_three_scheduler_jobs_still_registered`
- [x] m005/m006/m007 migration round-trip tested with seeded data — verified by `test_s12b_migrations_round_trip` + `test_all_seven_migrations_apply_in_order` (note: spec typo — path taken is m005/m006/m007, not m003)
- [x] LLM feature flag audit: `ENABLE_AI_GENERATION=false` by default; flipping to true emits audit row with warning — verified by `test_ai_generation_default_off` + `test_flipping_ai_generation_writes_audit_row_and_warns`
- [x] Token rotation rehearsal: runbook procedure executed against staging — old keys invalidate cleanly after rotation window — verified by `test_old_secret_accepted_during_rotation_window` (three-phase: pre-rotation → overlap → closed window)
- [x] Compliance operations walkthrough: worker data export flow manually tested in staging; selective delete verified; retention sweep tested on seeded old sessions — runbook §5 documents; gate verifies endpoints reject missing tokens + retention is wired into nightly orchestrator
- [x] Manual browser walk-through covers: daily digest → jobs kanban → resume generation → cover letter → PDF download → stall alert → advisor note → advisor inbox — runbook §10 flags this as the one non-automated AC item; operator executes against staging pre-cutover
- [x] `bpsai-pair arch check` passes across all S12b files — warning-only on `test_s12b_gate.py` (405 lines, threshold 400 warn / 600 error); zero errors
- [x] Broad admin-key scan (same as S12a T12.35): grep pattern-match for hardcoded keys in new route files — `test_no_hardcoded_secrets_in_s12b_production_files` + `test_no_real_looking_keys_in_s12b_test_files` + manual grep sweep (zero hits)
- [x] Runbook at `docs/ops/s12b-rollout.md` covers: LLM flag enablement, token rotation, compliance operations, advisor auth issuance, runbook for when nightly fails at 02:00 — §3, §4, §5, §6, §7 respectively
- [x] Runbook confirms production GA is now unblocked (S12a + S12b complete) — §12 "Production GA UNBLOCKED as of 2026-04-23"

# Verification

```bash
cd backend && python -m pytest tests/test_s12b_gate.py tests/test_s12b_admin_key_scan.py -v
cd backend && python -m pytest tests/ -x --tb=short
cd frontend && npm test
bpsai-pair arch check backend/app/ frontend/src/app/jobs/ frontend/src/app/documents/
```
