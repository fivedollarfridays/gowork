---
id: T22.2
title: Postgres driver + dual-DB config
plan: plan-2026-05-s22-identity-foundation
type: feature
priority: P0
complexity: 20
status: done
sprint: '22'
tags:
- identity
- sprint-22
- postgres
- config
depends_on:
- T22.1
completed_at: '2026-05-06T16:54:16.681763'
---

# Objective

Add `asyncpg` (and `psycopg[binary]` for Alembic sync ops) to project dependencies. Update `backend/app/core/database.py` to accept both `sqlite+aiosqlite://` and `postgresql+asyncpg://` URLs via `DATABASE_URL`. Update `backend/app/core/config.py` to expose `DB_DIALECT` (inferred from URL). Parameterize `backend/tests/conftest.py` so the entire 279-test backend suite runs against both engines.

# Files

- `backend/pyproject.toml` (or requirements equivalent)
- `backend/app/core/database.py`
- `backend/app/core/config.py`
- `backend/tests/conftest.py`

# Acceptance Criteria

- [x] `asyncpg` and `psycopg[binary]` pinned in `backend/pyproject.toml`
- [x] `DATABASE_URL` accepts `sqlite+aiosqlite:///` and `postgresql+asyncpg://`
- [x] `DB_DIALECT` config inferred from URL prefix
- [x] `backend/tests/conftest.py` exposes a `db_engine` fixture parameterized over both engines
- [x] All 279 backend tests pass against both engines locally
- [x] No callsite changes outside `database.py` / `config.py` / `conftest.py`
- [x] `bpsai-pair arch check` passes on touched files

# Verification

- `cd backend && pytest` (sqlite default)
- `cd backend && DATABASE_URL=postgresql+asyncpg://... pytest` (postgres path)
- `bpsai-pair arch check backend/app/core/database.py backend/app/core/config.py backend/tests/conftest.py`
- `bpsai-pair task update T22.2 --status done`

# Dependencies

- T22.1 — alembic env.py needs to reference dual-DB URL handling that lives in T22.2's database.py refactor.