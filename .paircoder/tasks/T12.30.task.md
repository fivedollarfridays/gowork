---
id: T12.30
title: Navigation + Stall Alert Banner
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P1
complexity: 10
status: pending
sprint: 12b
tags:
  - frontend
depends_on:
  - T12.26
  - T12.29
  - T12.27
  - T12.28
---

# Objective

Update `frontend/src/app/layout.tsx` to add nav links to Appointments, Jobs, Documents, Daily, Case Manager. Add a stall alert banner that shows only when a HARD stall is present. Dismiss persists 24h via localStorage. Mobile-responsive + keyboard-accessible.

# Files to Update

| File | Change |
|------|--------|
| frontend/src/app/layout.tsx | add nav links |
| frontend/src/components/NavBar.tsx | create or extend |
| frontend/src/components/StallAlertBanner.tsx | create |
| frontend/src/lib/translations/en.json | add nav + banner strings |
| frontend/src/lib/translations/es.json | add Spanish translations |
| frontend/src/__tests__/layout/ | create |

# Implementation Plan

1. Extend layout with a nav bar containing all 5 links.
2. Ensure keyboard accessibility (focus order, aria labels).
3. Build StallAlertBanner that renders only on HARD stall.
4. Dismiss button writes to localStorage with 24h TTL.
5. Mobile-responsive layout (hamburger or stacked).
6. Tests cover banner show/hide + dismiss persistence.

# Acceptance Criteria

- [ ] Nav links present and keyboard-accessible
- [ ] Stall banner shows only when applicable
- [ ] Dismiss persists 24h via localStorage
- [ ] Mobile-responsive
- [ ] Tests cover banner show/hide states

# Verification

```bash
cd frontend && npm test -- src/__tests__/layout
cd frontend && npm run lint
```
