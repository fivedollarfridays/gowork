---
id: T26.11
title: /admin landing page (index of admin sections + quick stats)
plan: plan-2026-05-s26-admin-dashboard
type: feature
priority: P1
complexity: 12
status: pending
sprint: '26'
tags:
- frontend
- sprint-26
- admin
- landing
depends_on:
- T26.8
- T26.9
- T26.10
---

# Objective

Index page at `/admin` listing all admin sections (S23 Assessments, S24 Listings, S25 Cities/DFW, S26 Resources/Feedback/BrightData) with quick-stats badges. Replaces the current URL-only navigation. Reads count badges from existing endpoints (cities-admin summary, listings-admin pending-claims, assessments-admin pending-reviews, plus S26's new flagged-resource + pending-visit-feedback counts).

# Files

- `frontend/src/app/admin/page.tsx` (new)
- `frontend/src/app/admin/__tests__/page.test.tsx` (new)

# Acceptance Criteria

- [ ] `frontend/src/app/admin/page.tsx`:
  - Quick-stats row at top: total flagged resources (T26.3 endpoint), total unreviewed visit-feedback (T26.3 endpoint), total pending claims (S24 endpoint), total pending assessments (S23 endpoint)
  - Section cards (one per admin sub-area):
    - Assessments → `/admin/assessments` (S23)
    - Listings → `/admin/listings` (S24)
    - Cities / DFW → `/admin/cities/dfw` (S25)
    - Resources → `/admin/resources` (T26.8)
    - Feedback → `/admin/feedback` (T26.9)
    - BrightData → `/admin/brightdata` (T26.10)
  - Each card: section name, brief description, count badge (when applicable), `<Link>` to the page
- [ ] `frontend/src/app/admin/__tests__/page.test.tsx` covers:
  - Section cards render (6 cards present)
  - Count badges fetch from APIs (mock all 4 count endpoints)
  - Links navigate to correct paths
- [ ] Page wrapped in `<RoleGate>` (inherited)
- [ ] `npx tsc --noEmit` clean
- [ ] `npx next build` green

# Verification

- `cd frontend && npx vitest run src/app/admin/__tests__/page.test.tsx`
- `cd frontend && npx tsc --noEmit && npx next build 2>&1 | tail -5`
- `bpsai-pair task update T26.11 --status done`

# Dependencies

- T26.8 — `/admin/resources` page must exist for the link
- T26.9 — `/admin/feedback` page must exist for the link
- T26.10 — `/admin/brightdata` page must exist for the link
