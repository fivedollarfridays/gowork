---
id: T24.10
title: Frontend Verified Listing badge component + integration
plan: plan-2026-05-s24-listing-verification
type: feature
priority: P0
complexity: 15
status: pending
sprint: '24'
tags: [verification, sprint-24, frontend, badge]
depends_on: [T24.6]
---

# Objective

`<VerifiedBadge tier={...} intakeComplete={...} />` renders three variants: source_trust (paler cyan, "Source Verified"), claim_verified ("Verified Employer"), admin_reviewed (same visual as claim_verified). intake_complete=true adds a "+ Intake Complete" sub-badge in amber. null/missing renders nothing. Integrate into the existing `/jobs` list page and detail page (if it exists).

Charter integrity: badge is display-only. Integration gate (T24.11) explicitly tests that the matching engine reads zero verification signals.

# Files

- `frontend/src/components/jobs/VerifiedBadge.tsx` (new)
- `frontend/src/app/jobs/page.tsx` (insertion — existing job list)
- `frontend/src/app/jobs/[jobId]/page.tsx` (insertion if it exists; otherwise list-only)
- `frontend/src/__tests__/jobs/verified-badge.test.tsx` (new)

# Acceptance Criteria

- [ ] `<VerifiedBadge tier intakeComplete />` renders three variants:
  - [ ] source_trust → small "Source Verified" badge, paler cyan, tooltip explains source-feed origin
  - [ ] claim_verified → "Verified Employer" badge full cyan, tooltip explains employer-claimed
  - [ ] admin_reviewed → same visual as claim_verified
- [ ] intake_complete=true adds "+ Intake Complete" amber sub-badge
- [ ] tier=null → renders nothing (returns null)
- [ ] Integrated into /jobs list; if /jobs/[jobId] exists, integrated there too
- [ ] vitest coverage: each tier renders correct text/style; intake_complete sub-badge appears only when true; null tier renders nothing; tooltip text per variant
- [ ] npx tsc --noEmit clean; npx next build green

# Verification

- `cd frontend && npx vitest run src/__tests__/jobs/verified-badge.test.tsx`
- `cd frontend && npx tsc --noEmit && npx next build`
- `bpsai-pair task update T24.10 --status done`

# Dependencies

- T24.6 — frontend reads `verification` field from the public listing fetch response.
