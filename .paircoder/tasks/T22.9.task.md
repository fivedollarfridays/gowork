---
id: T22.9
title: Anonymous-first invariant test
plan: plan-2026-05-s22-identity-foundation
type: feature
priority: P0
complexity: 15
status: done
sprint: '22'
tags:
- identity
- sprint-22
- invariant
- test
depends_on:
- T22.5
completed_at: '2026-05-06T17:29:58.929786'
---

# Objective

Ship a property test that, for every API route currently using `session_id` (35+ endpoints across `backend/app/routes/`), runs the endpoint twice — once with an anonymous session, once with a session claimed by an account — and asserts the responses are equivalent (modulo account-aware response fields like `account_id`). This is the load-bearing guard that prevents future drift toward forced-login.

# Files

- `backend/tests/test_anonymous_first_invariant.py` (new)

# Acceptance Criteria

- [x] `backend/tests/test_anonymous_first_invariant.py` enumerates all routes consuming `session_id` (auto-discovery via FastAPI route inspection — not hand-maintained list)
- [x] For each route, runs anonymous-fixture + claimed-fixture and asserts response equivalence (excluding allowlist of account-only fields: `account_id`, `account_email`)
- [x] Test failure on any new route that requires authentication (unless explicitly added to a documented allowlist with reason)
- [x] CI fails if invariant breaks
- [x] Documents the invariant in a top-of-file docstring referencing the integrity charter

# Verification

- `cd backend && pytest tests/test_anonymous_first_invariant.py -v`
- Confirm 35+ routes covered (count visible in test output)
- `bpsai-pair arch check backend/tests/test_anonymous_first_invariant.py`
- `bpsai-pair task update T22.9 --status done`

# Dependencies

- T22.5 — needs accounts table so the test can construct a "claimed session" fixture for comparison.