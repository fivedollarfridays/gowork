---
id: T23.1
title: 'Alembic migration: assessments + versions + questions + reviews tables'
plan: plan-2026-05-s23-assessment-authoring
type: feature
priority: P0
complexity: 25
status: done
sprint: '23'
tags:
- assessments
- sprint-23
- schema
- migration
depends_on: []
completed_at: '2026-05-07T08:42:59.892066'
---

# Objective

Add the four-table identity-of-content layer for the assessment authoring pipeline. Alembic revision 0013 with `down_revision='0012'`. Uses SQLAlchemy Core MetaData mirroring S22's `accounts_schema.py` pattern so DDL is dialect-correct on both engines without translation. The `assessments` row is the durable identity (slug, kind, track); each `assessment_version` is a frozen-on-publish snapshot; `assessment_questions` belong to a version (not the assessment) so revisions can change question content without breaking past scores; `assessment_reviews` records the review trail.

# Files

- `backend/alembic/versions/0013_assessments.py` (new)
- `backend/app/core/assessments_schema.py` (new)

# Acceptance Criteria

- [x] `backend/alembic/versions/0013_assessments.py`: `revision='0013'`, `down_revision='0012'`
- [x] `backend/app/core/assessments_schema.py` defines four `Table` objects on a shared `MetaData` (consider sharing with `accounts_schema.metadata` so FKs resolve cleanly)
- [x] `assessments(id PK, slug VARCHAR(160) UNIQUE, kind, track, created_by FK accounts(id), created_at, retired_at NULL)`
- [x] `assessment_versions(id PK, assessment_id FK, version_number INT, status, drafted_by FK accounts, reviewed_by FK accounts NULL, approved_by FK accounts NULL, published_at NULL, retired_at NULL, created_at)` with UNIQUE(assessment_id, version_number)
- [x] `assessment_questions(id PK, version_id FK, position INT, prompt TEXT, kind, rubric_json TEXT, scoring_weight REAL DEFAULT 1.0)` with UNIQUE(version_id, position)
- [x] `assessment_reviews(id PK, version_id FK, reviewer_id FK accounts, action, comment TEXT, created_at)`
- [x] `kind` ENUM enforced via CHECK constraint: `skill_probe | situational | knowledge_check | work_sample`
- [x] `track` ENUM enforced via CHECK constraint: `vocational | dao_tech | generic`
- [x] `assessment_versions.status` ENUM via CHECK: `draft | in_review | approved | published | retired | rejected`
- [x] `assessment_questions.kind` ENUM via CHECK: `mcq | freeform | code | scenario`
- [x] `assessment_reviews.action` ENUM via CHECK: `approve | reject | request_revision`
- [x] `apply_ddl(connection)` helper scoped to the four tables for test fixtures
- [x] Index on `(track, status)` on assessment_versions for the reviewer-queue hot path
- [x] `alembic upgrade head` runs clean on fresh sqlite + postgres (verify locally and in CI)
- [x] `bpsai-pair arch check backend/app/core/assessments_schema.py` passes

# Verification

- `cd backend && rm -f /tmp/_t23_test.db && DATABASE_URL=sqlite+aiosqlite:////tmp/_t23_test.db .venv/bin/alembic upgrade head` — runs clean
- Inspect: `sqlite3 /tmp/_t23_test.db ".schema assessments"` shows expected columns + CHECK constraints
- `bpsai-pair arch check backend/app/core/assessments_schema.py`
- `bpsai-pair task update T23.1 --status done`

# Dependencies

None — sprint entry task.