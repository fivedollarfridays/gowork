---
id: T22.6
title: account_roles + reviewer permission scaffold
plan: plan-2026-05-s22-identity-foundation
type: feature
priority: P1
complexity: 15
status: done
sprint: '22'
tags:
- identity
- sprint-22
- roles
- auth
depends_on:
- T22.5
completed_at: '2026-05-06T17:24:46.382063'
---

# Objective

Add the role enum (`case_manager`, `sme_reviewer`, `dao_reviewer`, `admin`) bound to accounts. Ship the `require_role(role)` FastAPI dependency that raises 403 if the requesting account lacks the required role. Admin-only is the working tier this sprint; full per-role test coverage and the reviewer dashboard UI ship in Sprint 23.

# Files

- `backend/alembic/versions/0012_account_roles.py` (new)
- `backend/app/core/queries_roles.py` (new)
- `backend/app/core/auth_roles.py` (new)
- `backend/tests/test_roles.py` (new)

# Acceptance Criteria

- [x] `backend/alembic/versions/0012_account_roles.py` creates `account_roles(account_id FK, role_name, granted_at, PRIMARY KEY(account_id, role_name))`
- [x] `role_name` constrained to enum: `case_manager` | `sme_reviewer` | `dao_reviewer` | `admin`
- [x] `backend/app/core/queries_roles.py` with `grant_role`, `revoke_role`, `list_roles_for_account`, `account_has_role`
- [x] `backend/app/core/auth_roles.py` exposes `require_role(role: str)` FastAPI dependency that returns 403 if account is anonymous OR lacks the role
- [x] `backend/tests/test_roles.py` covers admin grant/revoke/check paths
- [x] One account can hold multiple roles (no exclusivity)
- [x] `bpsai-pair arch check` passes on touched files

# Verification

- `cd backend && pytest tests/test_roles.py` (sqlite + postgres)
- `bpsai-pair arch check backend/app/core/queries_roles.py backend/app/core/auth_roles.py backend/tests/test_roles.py`
- `bpsai-pair task update T22.6 --status done`

# Dependencies

- T22.5 — roles bind to accounts; needs accounts table to FK into.