---
id: T26.6
title: Frontend admin_feedback API client
plan: plan-2026-05-s26-admin-dashboard
type: feature
priority: P0
complexity: 10
status: pending
sprint: '26'
tags:
- frontend
- sprint-26
- api-client
depends_on:
- T26.3
---

# Objective

Typed API client for the admin feedback endpoints (flagged-queue + visits-inbox). Same shape as T26.5 — imports the shared helpers (`fetchWithCookie` + `throwOnApiError`); one typed error class. Each function maps 1:1 to a backend endpoint from T26.3.

# Files

- `frontend/src/lib/api/admin_feedback.ts` (new)
- `frontend/src/lib/api/__tests__/admin_feedback.test.ts` (new)

# Acceptance Criteria

- [ ] `frontend/src/lib/api/admin_feedback.ts`:
  - Imports `fetchWithCookie` + `throwOnApiError` from `_client.ts`
  - Typed wire interfaces: `FlaggedResource`, `VisitFeedback`, `VisitFeedbackListResponse` (with pagination)
  - `AdminFeedbackApiError extends Error` with `status` + `detail`
  - Functions: `listFlagged(city)`, `approveFlagged(resourceId)`, `confirmHide(resourceId)`, `listVisits(opts: {reviewed?, limit?, offset?})`, `markVisitReviewed(visitId, actionTaken?)`
- [ ] All functions delegate to shared helpers (same pattern as T26.5)
- [ ] `frontend/src/lib/api/__tests__/admin_feedback.test.ts` — vitest URL/method/body assertions per function; typed error throws on non-2xx
- [ ] `npx tsc --noEmit` clean
- [ ] vitest suite green

# Verification

- `cd frontend && npx vitest run src/lib/api/__tests__/admin_feedback.test.ts`
- `cd frontend && npx tsc --noEmit`
- `bpsai-pair task update T26.6 --status done`

# Dependencies

T26.3 — needs the backend route shape.
