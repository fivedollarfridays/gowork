---
id: T23.2
title: queries_assessments.py CRUD module
plan: plan-2026-05-s23-assessment-authoring
type: feature
priority: P0
complexity: 20
status: done
sprint: '23'
tags:
- assessments
- sprint-23
- crud
depends_on:
- T23.1
completed_at: '2026-05-07T09:08:04.290022'
---

# Objective

The CRUD surface consumed by every backend endpoint in this sprint. Async SQLAlchemy with named bind params (driver-agnostic). Status transitions enforced at the query layer (you can't publish a non-approved version; you can't review a published version). Track-aware reviewer-queue filtering: case_manager sees vocational + generic, dao_reviewer sees dao_tech + generic, sme_reviewer sees all.

# Files

- `backend/app/core/queries_assessments.py` (new)
- `backend/tests/test_queries_assessments.py` (new)

# Acceptance Criteria

- [x] `create_assessment(session, slug, kind, track, created_by) -> int`
- [x] `create_draft_version(session, assessment_id, drafted_by, questions: list[dict]) -> int` — atomic insert of version + questions
- [x] `list_pending_reviews(session, *, reviewer_role: str) -> list[dict]` — track-aware filter; status in (draft, in_review, request_revision)
- [x] `get_version_with_questions(session, version_id) -> dict | None` — version row + questions ordered by position
- [x] `record_review(session, version_id, reviewer_id, action, comment) -> int` — inserts review row + transitions version status
- [x] `publish_version(session, version_id, approved_by) -> None` — guards status==approved; sets status=published + published_at; raises ValueError otherwise
- [x] `get_published_version(session, slug) -> dict | None` — latest published version + questions
- [x] `retire_version(session, version_id) -> None` — admin-only path (no route this sprint)
- [x] State-machine guards enforced at query layer: only approved → published; rejected terminates; published is immutable
- [x] All queries work on both sqlite and postgres
- [x] `backend/tests/test_queries_assessments.py` covers happy path + invalid-status-transition guards (24+ tests)
- [x] `bpsai-pair arch check` passes on touched files

# Verification

- `cd backend && .venv/bin/python -m pytest tests/test_queries_assessments.py -v`
- `bpsai-pair arch check backend/app/core/queries_assessments.py backend/tests/test_queries_assessments.py`
- `bpsai-pair task update T23.2 --status done`

# Dependencies

- T23.1 — schema tables must exist before CRUD can target them.