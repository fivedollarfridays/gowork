---
id: T24.2
title: queries_employers + queries_listings_verification + queries_listings_reputation
  CRUD modules
plan: plan-2026-05-s24-listing-verification
type: feature
priority: P0
complexity: 25
status: done
sprint: '24'
tags:
- verification
- sprint-24
- crud
depends_on:
- T24.1
completed_at: '2026-05-08T08:39:19.122645'
---

# Objective

Three CRUD modules consumed by every backend endpoint in this sprint. Async SQLAlchemy with `text()` + named bind params (driver-agnostic). Status transitions enforced at the query layer. State-machine guards: claim must be unused + unexpired before mark_claim_used; verification can transition source_trust → claim_verified → admin_reviewed (one-way, no downgrade).

# Files

- `backend/app/core/queries_employers.py` (new)
- `backend/app/core/queries_listings_verification.py` (new)
- `backend/app/core/queries_listings_reputation.py` (new)
- `backend/tests/test_queries_employers.py` (new)
- `backend/tests/test_queries_listings_verification.py` (new)
- `backend/tests/test_queries_listings_reputation.py` (new)

# Acceptance Criteria

- [x] `queries_employers`: `create_employer_account(session, *, name, domain) -> int`, `get_employer_by_domain`, `get_employer_by_id`, `list_pending_admin_review`
- [x] `queries_listings_verification`: `mint_listing_claim_token(session, *, listing_id, claimant_email, claimant_account_id) -> tuple[str, int]` (raw_token, claim_id; SHA-256 stored), `find_unused_claim_by_hash` (single helper, None for any failure mode), `mark_claim_used`, `create_verification(session, *, listing_id, employer_account_id, tier, verified_by)`, `set_intake(session, *, listing_id, intake_json)`, `get_verification_for_listing`, `get_public_verification_summary(session, listing_ids: list[int]) -> dict[int, dict]` (intake_json EXCLUDED)
- [x] `queries_listings_reputation`: `record_event(session, *, listing_id, event_kind, recorded_by, session_id=None, notes=None)`, `get_signal_rates(session, listing_id, window_days=30)` (stub for T24.8 implementation), `aggregate_for_employer`
- [x] State-machine guards raise ValueError; route layer translates to 409
- [x] All queries use text() + named binds + RETURNING id; portable on both engines
- [x] 35+ tests across the three test files
- [x] bpsai-pair arch check passes on touched files

# Verification

- `cd backend && .venv/bin/python -m pytest tests/test_queries_employers.py tests/test_queries_listings_verification.py tests/test_queries_listings_reputation.py -v`
- `bpsai-pair arch check backend/app/core/queries_employers.py backend/app/core/queries_listings_verification.py backend/app/core/queries_listings_reputation.py`
- `bpsai-pair task update T24.2 --status done`

# Dependencies

- T24.1 — schema tables must exist before CRUD can target them.