---
id: T13.128
title: Stand Up Staging Environment
plan: plan-2026-04-s13-platform-qc
type: docs
priority: P0
complexity: 25
status: pending
sprint: '13'
tags:
- qc
- security
- submission
---

# Objective

Provision a staging environment (recommend: same stack as prod target — Docker Compose host, managed Postgres if migrating off SQLite, or sticking with SQLite + persistent volume if intentional). Deploy current main. Smoke-test every top-level route. Hook to a staging.* DNS subdomain. Document deploy + rollback in runbook.

# Acceptance Criteria

- [x] Staging deployed at a stable URL (https://montgowork-staging-api.fly.dev + https://montgowork-staging-web.fly.dev)
- [x] DB seeded with demo data via T13.2 + T13.3 (10 demo sessions on Fly volume; T13.3 reset CLI not yet copied into container — followup)
- [ ] Every top-level route returns 200 in smoke check (15/19 PASS; 4 sub-fix findings logged: /health/ready RAG index, /api/admin/flags path, /documents + /feedback Next.js pages)
- [x] Deploy + rollback runbook in `docs/runbooks/staging-deploy.md`
- [x] `.paircoder/qc/config.yaml` `staging` profile points at the staging URL (env-var resolved: STAGING_FRONTEND_URL + STAGING_BACKEND_URL)
