---
id: T12.22
title: Daily Progress Module (Retro)
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 20
status: pending
sprint: 12a
tags:
  - backend
  - plan
  - retro
depends_on:
  - T12.23
---

# Objective

Create `backend/app/modules/plan/daily_progress.py`. `run_nightly_retro(session_id, for_date)` compares plan's expected actions vs actual evidence (from T12.23). Port `ops:lib/nightly_retro.py` (`collect`, `persist`, `load`, `collect_for_date`, `_derive_done_flags`, `RetroResult`).

# Files to Update

| File | Change |
|------|--------|
| backend/app/modules/plan/daily_progress.py | create — retro |
| backend/tests/test_daily_progress.py | create |

# Implementation Plan

1. Implement `run_nightly_retro(session_id, for_date)` for single session + date.
2. Source evidence from T12.23 — do NOT re-implement signal collection.
3. Classify each action as done/undone/partial.
4. Persist snapshot to `daily_progress_snapshots` table (T12.1).
5. Port helpers: `collect`, `persist`, `load`, `collect_for_date`, `_derive_done_flags`, `RetroResult`.
6. Write tests covering no-evidence, full-evidence, mixed cases.

# Acceptance Criteria

- [ ] Retro runs for a single session + date and returns structured result
- [ ] Evidence sourced from T12.23 (NOT re-implementing signal collection)
- [ ] Classification handles done/undone/partial
- [ ] Snapshot persisted to `daily_progress_snapshots`
- [ ] Tests cover no-evidence, full-evidence, mixed cases
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_daily_progress.py -v
bpsai-pair arch check backend/app/modules/plan/daily_progress.py
```
