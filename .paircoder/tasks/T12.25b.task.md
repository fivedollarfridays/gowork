---
id: T12.25b
title: Module Status Contracts
plan: plan-2026-04-s12b-value-extensions
type: feature
priority: P2
complexity: 20
status: done
sprint: 12b
tags:
- backend
- observability
depends_on:
- T12.11
- T12.15
- T12.16
- T12.19
completed_at: '2026-04-23T21:48:06.880827'
---

# Objective

Port ops `nightly_status()` self-reporting pattern. Each module exports `nightly_status(session_id) -> ModuleStatus`. Digest + advisor inbox consume aggregated status via `status_collector.collect_all(session_id)`.

# Files to Update

| File | Change |
|------|--------|
| backend/app/modules/common/temporal_types.py | add `ModuleStatus` model |
| backend/app/modules/documents/resume_builder.py | add `nightly_status()` |
| backend/app/modules/documents/cover_letter_builder.py | add `nightly_status()` |
| backend/app/modules/jobs/applications.py | add `nightly_status()` |
| backend/app/modules/engagement/reminder_engine.py | add `nightly_status()` |
| backend/app/modules/status_collector.py | create — aggregator |
| backend/tests/test_module_status.py | create |

# Implementation Plan

1. Add `ModuleStatus` Pydantic model: (module_name, health, signals, last_activity_at).
2. Implement `nightly_status(session_id)` on resume_builder, cover_letter_builder, applications, engagement.
3. Build `status_collector.collect_all(session_id)` aggregator.
4. Wire aggregated output into digest composer + advisor inbox (T12.31).
5. Tests cover each module's status under healthy + degraded conditions.

# Acceptance Criteria

- [x] `ModuleStatus` in `common/temporal_types.py`: (module_name, health, signals, last_activity_at)
- [x] `nightly_status(session_id)` implemented on: resume_builder (T12.15), cover_letter_builder (T12.16), job applications (T12.11), engagement (T12.19)
- [x] Aggregated via `status_collector.collect_all(session_id)`
- [x] Tests cover each module's status under healthy + degraded conditions
- [x] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_module_status.py -v
bpsai-pair arch check backend/app/modules/status_collector.py
```