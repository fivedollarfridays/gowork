---
id: T23.9
title: Postgres test transaction-per-test isolation
plan: plan-2026-05-s23-assessment-authoring
type: chore
priority: P1
complexity: 20
status: done
sprint: '23'
tags:
- test-infra
- sprint-23
- postgres
- follow-up
depends_on: []
completed_at: '2026-05-07T08:50:43.585353'
---

# Objective

Closes the deferred S22 follow-up. Replaces the `DROP SCHEMA public CASCADE` teardown (which lost races against pooled connections in CI) with a transaction-per-test pattern: each test runs inside `BEGIN ... ROLLBACK`, schema setup happens once per session via a session-scoped fixture. Identity-layer tests (test_accounts, test_roles, test_auth_*, test_anonymous_first_invariant) get re-added to the postgres CI scope.

This is independent of the assessment-authoring chain — can run in parallel with any other Wave 0 task.

# Files

- `backend/tests/conftest.py` (rewrite db_engine postgres axis)
- `.github/workflows/ci.yml` (re-add identity tests to postgres job)

# Acceptance Criteria

- [x] `db_engine` fixture postgres axis rebuilt:
  - [x] Session-scoped fixture creates schema via alembic once per pytest session (not per test)
  - [x] Per-test fixture wraps the test in `BEGIN ... ROLLBACK` so data state never leaks across tests
  - [x] Sqlite axis behavior unchanged (still per-test tmp file with full init_db chain)
- [x] `.github/workflows/ci.yml` postgres job adds: `test_accounts.py`, `test_roles.py`, `test_auth_magic_link.py`, `test_auth_claim.py`, `test_auth_me.py`, `test_anonymous_first_invariant.py`
- [x] CI passes both axes locally (with docker postgres) + on push
- [x] Comment in `ci.yml` updated to remove the deferred-follow-up note (since T23.9 closes it)
- [x] `bpsai-pair arch check backend/tests/conftest.py` passes

# Verification

- Locally with docker postgres: `cd backend && GOWORK_TEST_POSTGRES_URL=postgresql+asyncpg://... .venv/bin/python -m pytest tests/test_accounts.py tests/test_roles.py tests/test_auth_*.py tests/test_anonymous_first_invariant.py -v`
- All tests pass on both axes
- Push and watch postgres CI job — both legacy and new tests green
- `bpsai-pair task update T23.9 --status done`

# Dependencies

None — independent of the assessment-authoring chain.