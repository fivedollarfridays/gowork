---
id: T12.21a
title: Digest Preview Endpoint
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 5
status: pending
sprint: 12a
tags:
  - backend
  - routes
  - engagement
depends_on:
  - T12.20
---

# Objective

Minimal slice of T12.21 (engagement API routes) required for frontend T12.29. Single endpoint: `GET /api/engagement/preview-digest?session_id=X` — renders today's digest via T12.20 without sending. Remaining four engagement endpoints land in S12b T12.21.

# Files to Update

| File | Change |
|------|--------|
| backend/app/routes/engagement_preview.py | create — single endpoint |
| backend/app/routes/__init__.py | register router |
| backend/tests/test_engagement_preview_route.py | create |

# Implementation Plan

1. Create `GET /api/engagement/preview-digest?session_id=X` returning html + text variants.
2. Add session ownership check.
3. Call T12.20 `compose_digest`; do NOT send.
4. Register router in `all_routers`.
5. Write tests: authenticated session, unauthorized cross-session, empty-state digest.

# Acceptance Criteria

- [ ] `GET /api/engagement/preview-digest?session_id=X` returns both html + text variants
- [ ] Session ownership check
- [ ] Uses T12.20 compose_digest; does NOT send
- [ ] Registered in `all_routers`
- [ ] Tests cover authenticated session, unauthorized cross-session, empty-state digest
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_engagement_preview_route.py -v
bpsai-pair arch check backend/app/routes/engagement_preview.py
```
