---
id: T12.32
title: E2E Integration Tests
plan: plan-2026-04-s12a-worker-companion-foundation
type: feature
priority: P0
complexity: 25
status: pending
sprint: 12a
tags:
  - backend
  - testing
  - e2e
depends_on:
  - T12.25
  - T12.26
  - T12.29
---

# Objective

Create `backend/tests/test_s12a_worker_companion_e2e.py`. Full flow tests for S12a scope: (1) create session + plan → verify pathway placeholders generated → fill in appointment date → mark attended → verify outcome recorded → verify barrier progress. (2) Stall scenario: create plan, no activity 4 days → run stall scan → verify SOFT level assigned and surfaced in digest. (3) Full nightly digest cycle: activity logged → retro runs → digest composed → email sent via direct SendGrid. (4) Test both AL and TX cities.

# Files to Update

| File | Change |
|------|--------|
| backend/tests/test_s12a_worker_companion_e2e.py | create — 4 E2E flows + contract asserts |

# Implementation Plan

1. Write flow (1): session + plan → placeholders → fill date → mark attended → outcomes recorded → barrier progress; test in AL and TX.
2. Write flow (2): no-activity 4-day stall → scan → SOFT level → digest surfaces it.
3. Write flow (3): full nightly cycle with mocked SendGrid.
4. Add route-collision assertion: `/api/job-applications` vs `/api/jobs/{id}`.
5. Add two-caller pathway hook test: exercise both `routes/pathway.py` and `routes/plan_intelligence.py`.
6. Add k-anonymity test: single-session segment returns `suppressed: true`.
7. Add auto-advance suppression test: inject `appointment_auto_advance` outcome and assert filtered within 48h.
8. Add city-scope test: seed 2 cities, invoke orchestrator for city A, assert city B untouched.
9. Add event-emission contract test: subscriber on `appointment.attended` verifies payload.
10. Ensure clean DB between tests; runtime < 60s.

# Acceptance Criteria

- [ ] Four E2E flows pass for both cities
- [ ] Mocked email sends verified for content + recipient
- [ ] Route-collision assertion: test POSTs `/api/job-applications` and asserts response shape differs from `/api/jobs/{id}`
- [ ] Two-caller pathway hook: placeholder generation via BOTH `routes/pathway.py` AND `routes/plan_intelligence.py`
- [ ] k-anonymity: community funnel test with single-session segment asserts `suppressed: true`
- [ ] Auto-advance suppression on stall: inject `appointment_auto_advance` outcome and assert stall detector filters it within 48h window
- [ ] City-scope on orchestrator: seed sessions in 2 cities, invoke orchestrator for city A, assert city B sessions untouched
- [ ] Event emission contract: test subscriber registered on `appointment.attended` verifies event fired with expected payload
- [ ] No test pollution (clean DB between tests)
- [ ] Runs in under 60 seconds
- [ ] `bpsai-pair arch check` passes

# Verification

```bash
cd backend && python -m pytest tests/test_s12a_worker_companion_e2e.py -v
bpsai-pair arch check backend/tests/test_s12a_worker_companion_e2e.py
```
