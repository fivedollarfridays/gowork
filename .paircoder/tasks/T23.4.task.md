---
id: T23.4
title: Reviewer queue API
plan: plan-2026-05-s23-assessment-authoring
type: feature
priority: P0
complexity: 25
status: pending
sprint: '23'
tags: [assessments, sprint-23, review, api]
depends_on: [T23.2]
---

# Objective

The reviewer-side surface: list pending drafts visible to the requesting reviewer's role, fetch one for review, record a review action. Publish is its own endpoint (T23.5) since publish authority is narrower than review authority. State machine enforced at the query layer (T23.2): draft → in_review → approved | request_revision; rejected terminates.

Lands in a NEW route module — must NOT extend `backend/app/routes/auth.py`.

# Files

- `backend/app/routes/assessments_review.py` (new)
- `backend/app/routes/__init__.py` (register new router)
- `backend/tests/test_assessments_review.py` (new)

# Acceptance Criteria

- [ ] `GET /api/admin/assessments/pending` — track-aware filter via `list_pending_reviews(reviewer_role)`
- [ ] `GET /api/admin/assessments/{version_id}` — returns full version + questions for review
- [ ] `POST /api/admin/assessments/{version_id}/review` — body `{action: approve|reject|request_revision, comment}`
- [ ] All routes gated: any of `case_manager` / `sme_reviewer` / `dao_reviewer` (use an `any_of_roles` helper or three-way dependency)
- [ ] `assessments_review_router` registered in `backend/app/routes/__init__.py:all_routers`
- [ ] Track filtering verified: case_manager seeing dao_tech draft → empty; dao_reviewer seeing vocational → empty; sme_reviewer sees all
- [ ] State-machine guards verified: review on published version → 409; review on rejected version → 409
- [ ] Tests cover: anonymous-403, wrong-role-403, correct-role-200 for each action
- [ ] `bpsai-pair arch check` passes

# Verification

- `cd backend && .venv/bin/python -m pytest tests/test_assessments_review.py -v`
- `cd backend && wc -l app/routes/auth.py` — confirm unchanged
- `bpsai-pair arch check backend/app/routes/assessments_review.py`
- `bpsai-pair task update T23.4 --status done`

# Dependencies

- T23.2 — review surface consumes queries_assessments CRUD (list_pending_reviews, get_version_with_questions, record_review).
