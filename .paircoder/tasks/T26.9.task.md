---
id: T26.9
title: '/admin/feedback page (tabbed: Flagged + Visits)'
plan: plan-2026-05-s26-admin-dashboard
type: feature
priority: P0
complexity: 25
status: pending
sprint: '26'
tags:
- frontend
- sprint-26
- admin
- feedback
depends_on:
- T26.6
---

# Objective

Single Next.js page at `/admin/feedback` with two tabs (shadcn `Tabs`): "Flagged Resources" and "Visit Feedback". Each tab paginates independently. Shared layout via the existing admin shell. The tab components are split into a separate `FeedbackTabs.tsx` for testability.

# Files

- `frontend/src/app/admin/feedback/page.tsx` (new)
- `frontend/src/components/admin/FeedbackTabs.tsx` (new — splits Flagged + Visits tabs)
- `frontend/src/app/admin/feedback/__tests__/page.test.tsx` (new)

# Acceptance Criteria

- [ ] `frontend/src/app/admin/feedback/page.tsx`:
  - shadcn `Tabs` component with two `TabsTrigger`s: "Flagged Resources" and "Visit Feedback"
  - Default tab = "Flagged Resources"; URL hash sync (`#flagged` / `#visits`) so deep links work
- [ ] `frontend/src/components/admin/FeedbackTabs.tsx`:
  - `FlaggedResourcesTab`: card-per-resource showing name + recent negative `resource_feedback` excerpts (last 30 days from T26.3 endpoint); row actions = Approve (calls `approveFlagged`) + Confirm Hide (calls `confirmHide`); refetches on action
  - `VisitFeedbackTab`: table with columns (submitted_at, made_it_to_center badge, plan_accuracy stars, free_text excerpt, reviewed badge, action_taken summary); filter dropdown (all/reviewed/unreviewed); row action = "Mark Reviewed" with optional `action_taken` textarea; submits via `markVisitReviewed`
  - Both tabs paginate independently; default 50 per page; prev/next controls
- [ ] `frontend/src/app/admin/feedback/__tests__/page.test.tsx` covers:
  - Tabs switch correctly (click TabsTrigger flips visible content)
  - Both lists render with mocked API responses
  - URL hash sync works (`#visits` activates VisitFeedbackTab on mount)
  - Approve/confirm-hide actions call correct API client functions
  - Mark-reviewed submits action_taken text
- [ ] Page wrapped in `<RoleGate>` (inherited from `/admin/layout.tsx`)
- [ ] `npx tsc --noEmit` clean
- [ ] `npx next build` green

# Verification

- `cd frontend && npx vitest run src/app/admin/feedback/__tests__/page.test.tsx`
- `cd frontend && npx tsc --noEmit && npx next build 2>&1 | tail -5`
- `bpsai-pair task update T26.9 --status done`

# Dependencies

T26.6 — needs the typed API client functions for flagged + visits.
