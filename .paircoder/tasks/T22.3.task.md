---
id: T22.3
title: Port m001-m010 hand-rolled DDL into Alembic versions
plan: plan-2026-05-s22-identity-foundation
type: feature
priority: P0
complexity: 30
status: done
sprint: '22'
tags:
- identity
- sprint-22
- migration
- alembic
depends_on:
- T22.1
- T22.2
completed_at: '2026-05-06T17:05:27.984718'
---

# Objective

Replay all 10 hand-rolled migrations as Alembic revisions in `backend/alembic/versions/`. Each `m00X_*.py` becomes `000X_*.py` with the same DDL semantics. After this task, `runner.py` is deprecated — calls forward to `alembic upgrade head` with a deprecation warning. Existing tests continue passing untouched. Both engines.

# Files

- `backend/alembic/versions/0001_initial.py` through `backend/alembic/versions/0010_geocode_resources_jobs.py` (10 new files)
- `backend/app/core/migrations/runner.py` (mark deprecated; redirect to alembic)
- `backend/app/core/schema.py` (re-export shim preserved for backward-compat)

# Acceptance Criteria

- [x] 10 alembic revisions created: `0001_initial.py` through `0010_geocode_resources_jobs.py`
- [x] Each revision's `upgrade()` produces schema byte-equivalent to `runner.apply_pending` on a fresh sqlite DB
- [x] Same equivalence on postgres (with type-mapping notes documented in PR if any)
- [x] `backend/app/core/migrations/runner.py` deprecated: emits `DeprecationWarning`, forwards to alembic
- [x] `backend/app/core/schema.py` `DDL_SQL` re-export still works (backward-compat)
- [x] All 279 tests still pass
- [x] `bpsai-pair arch check backend/alembic/versions/` passes

# Verification

- Compare schema dump between `runner.apply_pending` and `alembic upgrade head` on fresh sqlite DBs (`sqlite3 schema.sqlite ".schema"` diff)
- Same on postgres (`pg_dump --schema-only` diff)
- `cd backend && pytest`
- `bpsai-pair arch check backend/alembic/versions/`
- `bpsai-pair task update T22.3 --status done`

# Dependencies

- T22.1 — alembic infrastructure must exist before versions land in it.
- T22.2 — postgres driver required for parity check during port.