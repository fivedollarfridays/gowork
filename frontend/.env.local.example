NEXT_PUBLIC_API_URL=http://localhost:8000

# EmailJS (optional - email export hidden when not configured)
NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=

# Mapbox public token (W2: required for The Wall map render). Public
# tokens (pk.*) are designed to ship in client bundles — Mapbox's docs
# explicitly say they're not secrets. The team token below is committed
# so anyone who clones the repo (Kevin, judges, future contributors)
# gets a working map out of the box. Rotate via the Mapbox dashboard
# after the demo + URL-restrict to gowork.vercel.app + localhost for
# production. Create your own at https://account.mapbox.com/access-tokens/
# if you'd rather use your own account.
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1IjoiamRvZ3NoYXduIiwiYSI6ImNtb2pndnozOTBjdTQycW9kOHpsM2ZtMjIifQ.Nb6SByXgNlHWIh7wHefDqQ

# Mapbox custom dark editorial style (W2 T2.18). Build the style once in
# Mapbox Studio (see docs/runbooks/mapbox-style-setup.md) and paste the
# resulting URI here. When unset, MapboxScene falls back to the stock
# `mapbox://styles/mapbox/dark-v11`. URIs MUST start with `mapbox://styles/`
# — anything else is rejected as a URL-spoofing defense.
NEXT_PUBLIC_MAPBOX_STYLE_URL=mapbox://styles/your-account/wall-dark-editorial-v1

# Live Now widget (W4 Driver A). ISO-8601 timestamp the operator stamps at
# each deploy — the Wall surfaces "calibrated <relative-time> ago" so
# judges see this is a live, recently-touched system. Update on every
# production deploy (the Vercel runbook lists this as a deploy step).
NEXT_PUBLIC_LAST_CALIBRATED=2026-04-28T00:00:00Z

# Canonical origin for absolute OG / Twitter image URLs (W4 Driver D
# wallMetadata.ts). When unset, the metadata helper falls back to
# path-relative URLs — Vercel will absolutize at request time, but social
# crawlers (Slack/Twitter/LinkedIn unfurl) want absolute. ALWAYS set in
# production.
NEXT_PUBLIC_SITE_URL=https://gowork.vercel.app
